# FIX_TRACKING — ระบบติดตามการแก้ไข + ตรวจจับ Regression

> ตอบโจทย์: *"แก้จุดนี้แล้ว จุดนี้ทำงานได้ แต่อีกจุดเสียแทน"* — ทุกการแก้ต้องรู้ว่า (1) แก้สำเร็จจริงไหม (2) ไปทำอะไรพังหรือเปล่า
> ใช้คู่กับ [ISSUES.md](ISSUES.md) (รายการบั๊ก) และ [tests/](../tests/README.md) (ระบบเทส Dry Run)

---

## Workflow บังคับ ทุกครั้งที่แก้โค้ด

```
1. ก่อนแก้   node tests/run.mjs --json > baseline.json     ← เก็บสถานะก่อนแก้
2. แก้โค้ด   (แก้ทีละ issue — อย่ารวมหลาย issue ใน commit เดียว)
3. หลังแก้   node tests/run.mjs                            ← ดูผล
4. อ่านผล:
   ├─ 🎉 KNOWN-FIXED [รหัส issue ที่แก้]  = แก้สำเร็จ → ทำข้อ 5
   ├─ ❌ FAIL ตัวใหม่ที่ไม่เคยพัง          = ⚠️ REGRESSION! การแก้ไปพังของเดิม
   │                                        → ห้ามไปต่อ แก้ให้เขียวก่อน หรือ revert
   └─ 🟡 KNOWN-OPEN เท่าเดิม              = ปกติ (บั๊กอื่นที่ยังไม่ถึงคิว)
5. เมื่อ KNOWN-FIXED:
   a. ย้ายเทสนั้นจาก knownIssue() → test()   ← กลายเป็นยามกัน regression ถาวร
   b. อัปเดตตารางสถานะข้างล่าง + ติ๊ก [x] ใน ISSUES.md
6. ทดสอบมือตาม checklist ของ "หน้าที่ได้รับผลกระทบ" (ดู Impact Map)
7. ให้ code-reviewer agent ตรวจ diff ก่อน commit
```

**กติกาเหล็ก:** เทสทั้งหมดเป็น **Dry Run** — ห้ามเขียนเทสที่ต่อ Supabase จริง ห้ามแก้/เพิ่ม/ลบข้อมูลหรือโครงสร้าง DB ในการเทสเด็ดขาด

---

## ตารางสถานะการแก้ไข

สถานะ: ⬜ รอ admin เลือก · 🔧 กำลังแก้ · ✅ แก้แล้ว(เทสผ่าน) · ⚠️ แก้แล้วแต่เกิด regression · 🚫 ไม่แก้ (ยอมรับ)

| Issue | เรื่อง | สถานะ | เทสคุ้มกัน (อัตโนมัติ) | ไฟล์ที่ต้องแตะ | จุดเสี่ยงพังตาม (ดู Impact Map) |
|---|---|---|---|---|---|
| C1 | service_role key ใน api.js | ✅ **ปิดงานสมบูรณ์** (โค้ด + RLS + revoke key เก่าแล้ว) | ✅ `unit/no-secret-keys` [C1-guard] + เทส migration 3 ข้อ | `Js/api.js`, `Js/settings-shared.js`, `Html/settings.html`, `docs/sql/016_rls_policies.sql` | **ทุกหน้า** — ทดสอบจริงผ่านแล้ว |
| C2 | Stored XSS หลายจุด | ✅ **เสร็จ** | ✅ `unit/xss-guard` × 7 (สแกน source อัตโนมัติ) | script.js, sidebar-shared, + escape ทุกไฟล์ 14 ตัว, settings/audit_check/sku_master/import_counts/count_search/book_explorer/dashboard | ทดสอบ payload จริงในเบราว์เซอร์แล้ว ไม่ execute |
| M25 | `getClient()` สร้าง client ใหม่ทุกครั้ง | ✅ **เสร็จ** | ✅ `unit/api-client-cache` × 3 | `Js/api.js` | **ทุกหน้า** — เดิม 6 GoTrueClient warning/โหลด เหลือ 0 |
| H1 | cycle_id เดือนเก่าค้าง | ✅ **เสร็จ** | ✅ `dryrun/active-cycle` [H1-guard] × 6 | `Js/reconcile-shared.js`, `Js/script.js`, `Js/sidebar-shared.js`, `Html/import_counts.html` + cache-buster ทุกหน้า | ทดสอบครบ 8 หน้าที่ใช้ reconcile-shared แล้ว |
| H2 | audit_check dedupe ลบข้อมูลถูกต้อง | ✅ **เสร็จ** | ✅ `unit/audit-dedupe` × 19 | `Js/audit-dedupe.js` (ใหม่), `Html/audit_check.html` | audit_check ทั้งหน้า — ทดสอบกับข้อมูลจริง 6,078 แถวแล้ว |
| H10 | audit_check ไม่เตือนแถวที่บวกซ้ำในรอบเดียวกัน (ผลข้างเคียงของ H2) | ✅ **เสร็จ** | ✅ `unit/audit-dedupe` [H10] × 13 | `Js/audit-dedupe.js` (+`classifyCycleDuplicate`), `Html/audit_check.html` (verifyRow/formatRefNote/classifyRefDuplicates + cache-buster `k`) | audit_check: กล่องสถิติ "ผิดพลาด" + ฟิลเตอร์สถานะ · ปุ่มลบอัตโนมัติ **ไม่เปลี่ยนพฤติกรรม** (ยังใช้กฎ H2) |
| H12 | ปุ่ม "ลบแถวที่กดบันทึกซ้ำ" ไม่เคยเลือกเคสอย่าง PC700 (ใช้กฎเข้มของ H2) | ✅ **เสร็จ** | ✅ `unit/audit-dedupe` [H12] × 10 | `Js/audit-dedupe.js` (+`findSameCycleDuplicates`), `Html/audit_check.html` (`dedupeInventoryCountsInDb` + ข้อความ popup 2 ขั้น + tooltip) | **ปุ่มลบข้อมูลจริง** — เกณฑ์กว้างขึ้น ต้องอ่าน popup ให้ครบก่อนกด · ข้ามกลุ่มที่ยืนยันว่าปกติแล้วเสมอ · กฎเดิม `findAccidentalDuplicates` ยังอยู่แต่ไม่ใช่ตัวตัดสินการลบแล้ว |
| ACK | ระบบต้อง "แจ้งให้คนยืนยัน" แทนการตัดสินลบเอง (นโยบาย admin) | ✅ **เสร็จ** | ✅ `unit/audit-book-impact` [ACK] × 5 | `docs/sql/019` (ใหม่ — **รันแล้ว 2026-08-10**), `Js/audit-book-impact.js` (+`classifyAcceptance`), `Html/audit_check.html` (ปุ่ม "ยืนยันว่าปกติ" + โหลด acceptances + หน้าประวัติการแก้ไข/ลบ) | ถ้ายังไม่รัน 019 ปุ่มยืนยันจะใช้ไม่ได้ (หน้าเตือนเอง) ส่วนอื่นทำงานปกติ · การยืนยันมีผลกับทั้งสถานะบนจอ **และ** การเลือกของปุ่มลบ |
| H11 | audit_check มองไม่เห็นแถวทับซ้อน (ตำแหน่งเดียวกัน จำนวนต่างกัน) | ✅ **เสร็จ** (ปรับนิยามใหม่ตามนโยบาย) | ✅ `unit/audit-book-impact` [H11] × 13 | `Js/audit-book-impact.js` (ใหม่), `Html/audit_check.html` (โหลด `reconciliation_lines`, สถานะ `overlap`, แถบสรุป, cache-buster ล่าสุด `20260810d`) | audit_check: กล่องสถิติเพิ่มเป็น 5 ช่อง + ฟิลเตอร์ใหม่ 2 ตัว (`overlap`, `impact`) · Export Excel ได้สถานะใหม่อัตโนมัติ · ถ้าโหลด `reconciliation_lines` ไม่ได้ หน้ายังตรวจข้อมูลนับได้ตามปกติ (แค่ไม่มีตัวเลขเทียบ Book) |
| LC1 | โหมด "เทียบตำแหน่ง Excel" บล็อกทุกแถว 100% — ใช้งานไม่ได้เลยตั้งแต่แรก | ✅ **เสร็จ** | ✅ `unit/audit-loc-compare` [LC] × 15 + `unit/audit-select-columns` [LC-guard] × 7 (สแกน source · ตัดคอมเมนต์ก่อนสแกน · พิสูจน์ด้วย mutation ทั้ง delete และ comment-out) | `Js/audit-loc-compare.js` (ใหม่ — ย้าย `buildLocComparePlan` ออกจาก HTML + `missingQty`), `Html/audit_check.html` (SELECT เพิ่ม `counted_qty, cycle_id` · dropdown คลังในโมดัล + ซิงก์ scope bar · ข้อความ block ที่บอกสาเหตุ · cache-buster `20260810f`) | **เส้นทางที่แก้ข้อมูลจริงจำนวนมาก** — เพิ่งเริ่มทำงานได้เป็นครั้งแรก ต้องทดสอบกับโซนเล็กก่อน · guard M26 เพิ่งมีผลจริงเมื่อ qty ถูกต้อง |
| M26 | `getDestinationCollision` ยังบล็อกตาม key เดิม | ✅ **เสร็จ** | ✅ `unit/audit-dedupe` [M26] × 6 | `Js/audit-dedupe.js` (+`classifyDestinationCollision`), `Html/audit_check.html` (+`getCycleForRecordId`, `validateDestUpdateBatch` คืน `warned`, กล่องยืนยันใหม่) | โหมดแก้ location / สลับ SKU↔loc / เทียบตำแหน่งจาก Excel — ทั้ง 3 เส้นทางใช้ `confirmDestUpdatesWithSkips` ร่วมกัน |
| H3 | audit_check ไม่เขียน audit log | ✅ **เสร็จ** (ส่วน atomic → M27) | ✅ `unit/audit-log` × 21 (มีตัวสแกนบังคับทุกฟังก์ชันที่ mutate) | `Js/audit-log.js` (ใหม่), `Html/audit_check.html`, `Js/script.js` | ปริมาณ log เพิ่ม → drawer ประวัติใน index (limit 100 → ดู M28) |
| M29 | `deleteCycle` ลบ adjustment ทั้งรอบด้วย FK CASCADE โดยไม่ log | ⬜ (พบใหม่จาก review H6) | — | `Js/reconcile-shared.js:1030-1038` | cycle_config: ปุ่มลบรอบ — เส้นทางที่ลบ applied ได้มากที่สุดในระบบ |
| M30 | log `RECONCILE_ADJ_CLEAR` ท่วม drawer ประวัติ (limit 100) | ⬜ (พบใหม่จาก review H6) | — | `Js/script.js:1787-1791` | index: drawer ประวัติการทำงาน |
| M27 | bulk update ยังไม่ atomic | ⬜ (แยกจาก H3) | — | `Html/audit_check.html` + RPC ใหม่ (ต้องขออนุมัติ) | ความเสี่ยงต่ำแล้วเพราะมี log ครบ |
| UI1 | เมนูซ้ายแตกบนจอเล็ก (CSS responsive หลุดจาก DOM ที่ JS สร้าง) | ✅ **เสร็จ** | ✅ `unit/sidebar-responsive` [sidebar-guard] × 6 (รวมยาม z-index + ตำแหน่งลิ้นชัก) + [asset-ver] × 3 — พิสูจน์ด้วย mutation แล้วว่าจับบั๊กเดิมได้จริง | `Css/style.css` (แทนที่ media 900px + สไตล์ bar/toggle/scrim + `--sidebar-bar-h` + overflow), `Js/sidebar-shared.js` (`renderMobileChrome` + open/close + matchMedia), `Css/chat-notify.css` (ดัน toast พ้นปุ่ม ☰), **13 HTML** (cache-buster `20260810a`: style.css + sidebar-shared.js + ui-confirm.css) | **ทั้ง 13 หน้า** — ทดสอบจริงในเบราว์เซอร์ที่ 375 / 598 / 1024×560 / 1200 / 1366×768 + hit-test ปุ่มปิด drawer ประวัติแล้ว |
| M22 | book_explorer ไม่มี `has-sidebar` | ✅ **เสร็จ** (พ่วง UI1) | — (ครอบด้วยสายตา + เทส asset-ver) | `Html/book_explorer.html` (`<body>` + padding `.main-content`) | book_explorer เท่านั้น |
| H4 | dashboard คูณ Book ต่อคลัง | ✅ **เสร็จ** | ✅ `unit/dashboard-shared` [H4-guard] × 8 | `Js/dashboard-shared.js`, `Html/dashboard.html` | dashboard แท็บ 1 — ทดสอบกับรอบ 2 คลังจริงแล้ว |
| H9 | `.range()` เรียงไม่เสถียร (พบตอนแก้ H4) | ✅ **เสร็จ** | ✅ `unit/stable-paging` (สแกน source ทั้งโปรเจกต์) | `reconcile-shared`, `script.js`, `live-count-wall`, `dashboard/sku_master/book_explorer` (10 จุด) | **ทุกหน้าที่โหลดข้อมูลเกิน 1,000 แถว** — reconcile presence map กระทบหนักสุด |
| H5 | reconcile เปลี่ยนรอบแล้วเขียนรอบเก่า | ✅ **เสร็จ** | ✅ `unit/reconcile-cycle-sync` [H5-guard] × 10 (สแกน call site) | `Html/reconcile.html`, `Js/reconcile-shared.js` (export `isoToBangkokYmd`) | ทุกปุ่มใน reconcile — ทดสอบสลับรอบ + คำนวณ Match จริงแล้ว |
| H6 | import force "ถูกต้อง" + ลบ adjustment applied ไร้หลักฐาน | ✅ **เสร็จ** | ✅ `dryrun/clear-adjustments` [H6-guard] × 9 | `Js/reconcile-shared.js` (+`importBookAndRecompute`, `logAdjustmentsBeforeDelete`, `rollbackAuditEntries`), `Html/reconcile.html` (call site + เลิก cache accept), `Js/script.js` (drawer รู้จัก action ใหม่), `docs/DATABASE.md` | reconcile: Excel import **และ** ลบรายการ Book (ทั้งสองทางลบ adjustment) · ⚠️ ยังเหลือ `deleteCycle` ที่ลบทั้งรอบด้วย FK CASCADE โดยไม่ log (ดู M29) |
| H7 | avgPerMin สูงเกินจริง | ✅ **เสร็จ** | ✅ `unit/dashboard-shared` [H7-guard] × 3 | `Js/dashboard-shared.js:63-70` | dashboard แท็บ 1 (KPI ความเร็ว) — peakPerMin ยืนยันแล้วว่าไม่เปลี่ยน |
| H8 | chat: okLabel/UI ค้าง/no-auth | ✅ **เสร็จ** | ✅ `unit/chat-clear-guard` [H8-guard] × 16 | `Html/chat.html` (เท่านั้น — ไม่แตะ shared JS จึงไม่ต้อง bump cache-buster) | chat: ปุ่มล้างแชท (3 ด่าน + สำรองอัตโนมัติ + ร่องรอยผู้ทำ) |
| M2 | สถานะ JS ≠ SQL | ✅ **เสร็จ** | ✅ `dryrun/match-status` [M2] × 4 (แปล CASE จาก SQL มารันเทียบ JS จริง) + `unit/reconcile-book` [M2] | `docs/sql/020_...sql` (ยึดฝั่ง JS แล้วแก้ SQL) | reconcile ทุกสถานะ, dashboard การ์ด "ความต่าง (ชิ้น)" + Top 15 เกิน — รันจริงแล้ว 77 แถวเปลี่ยน |
| M3 | แถว "ขาด" แสดง `+N` + แถวรวม export ไร้ความหมาย | ✅ **เสร็จ** | ✅ `dryrun/match-status` [M3] × 5 | `Html/reconcile.html` (`computeDisplayVariance`, `sign`, `buildMatchExportRows`, `buildAdjustedExportRows`) | reconcile ตาราง + แผงปรับยอด + Export ทั้ง 2 ชีต |
| M18 | `%` ใช้ค่าเก่าจาก DB ขัดกับคอลัมน์ "ต่าง" | ✅ **เสร็จ** | ✅ `dryrun/match-status` [M18] × 3 | `Html/reconcile.html` (`formatRowVariancePct`) | reconcile ตาราง (คอลัมน์ %) + Export ชีต Match |
| M4 | name_pro โดน UPPERCASE | ✅ **เสร็จ** | ✅ `unit/reconcile-book` [M4] × 3 (ย้ายจาก knownIssue) | `Js/reconcile-shared.js` (`parseBookExcelRows`) | cycle_config upload Book, reconcile import + ปุ่ม "สร้างลง Book" — ⚠️ **ชื่อใน DB เก่ายัง UPPERCASE อยู่** (แก้โค้ดไม่ได้แก้ข้อมูลเก่า) |
| M19 | encode คลังไม่เสถียร | ✅ **เสร็จ** (เหลือ M33) | ✅ `unit/reconcile-warehouse` [M19] × 4 | `Js/reconcile-shared.js` (`encodeCycleWarehouses`) | cycle_config สร้าง/แก้รอบ |
| M24 | รอบ `closed/archived` ยังรับผลนับใหม่ | ✅ **เสร็จ** | ✅ `dryrun/cycle-scope` [M24] × 5 | `Js/reconcile-shared.js` (`isCycleClosed`) + `Js/script.js` (`filterCyclesCurrentMonth`) | index dropdown รอบ + บันทึกผลนับ + import_counts |
| M1 | วัน/เดือนที่มีข้อมูลนับหายจาก dropdown | ✅ **เสร็จ** | ✅ `dryrun/cycle-scope` [M1] × 8 | `Js/reconcile-shared.js` (`fetchCountMonths`/`fetchCountDaysInMonth`) + `Html/cycle_config.html` + `Html/count_search.html` | cycle_config ผูกผลนับ, count_search โหลดเดือน |
| M อื่น ๆ / L | ดู [ISSUES.md](ISSUES.md) | ⬜ | เพิ่มเทสเมื่อเริ่มแก้ | — | — |

> **ธรรมเนียม:** issue ไหนเริ่มแก้ ให้เพิ่มเทส (อย่างน้อย 1 knownIssue → test) ก่อนหรือพร้อมกับการแก้เสมอ — ถ้า logic อยู่ inline ใน HTML ให้พิจารณาย้ายฟังก์ชันนั้นลง `Js/*.js` เพื่อให้เทสได้

---

## ✅ Regression C1 (พบและแก้แล้ว 2026-08-09) — เคสตัวอย่าง "แก้จุดนี้ อีกจุดเสียแทน"

> **สรุป: แก้เรียบร้อยแล้ว** ด้วย `docs/sql/016_rls_policies.sql` (รันแล้ว) — ทุกตารางกลับมาอ่าน/เขียนได้ครบ ยืนยันด้วยการทดสอบหน้าจริง
> เก็บบันทึกไว้เป็นกรณีศึกษาว่ากลไกตรวจจับทำงานอย่างไร และทำไม smoke แบบผิวเผินถึงพลาด

**อาการ:** เปลี่ยนจาก service_role → publishable key แล้ว **ตารางส่วนใหญ่คืนค่าว่างเปล่าเงียบ ๆ** (HTTP 200 + 0 แถว ไม่มี error)

**สาเหตุ:** service_role **ข้าม RLS ทั้งหมด** ส่วน anon/publishable **ไม่ข้าม** — ตารางเหล่านี้เปิด RLS ไว้แต่**ไม่มี policy สักข้อ** ผลคือ anon ถูกปฏิเสธทุกแถว

| ตาราง | แถวจริงใน DB | anon อ่านได้ | ผลต่อระบบ |
|---|---|---|---|
| `warehouses` | 4 | **0** ❌ | dropdown คลังว่างทุกหน้า (ตกไปใช้ fallback hardcode 3 คลัง) |
| `count_cycles` | 6 | **0** ❌ | เลือกรอบไม่ได้ → index/cycle_config/reconcile/dashboard ใช้งานไม่ได้ |
| `book_stock_lines` | 5,326 | **0** ❌ | Book หายหมด → KPI = 0%, autocomplete ว่าง, book_explorer ว่าง |
| `reconciliation_lines` | 5,628 | **0** ❌ | หน้า reconcile + dashboard แท็บ Match ว่าง |
| `stock_adjustments` / `..._match_acceptances` | 0 / 0 | ❌ | ปรับยอด/ยืนยันถูกต้อง ใช้ไม่ได้ |
| `chat_messages` | 19 | **0** ❌ | แชทว่าง |
| ~~`sku_master`~~ | 1,179 | ✅ SELECT | 🚫 **ฟีเจอร์ถอดออกจากเว็บแล้ว 2026-08-10** — ตารางยังอยู่ในฐาน ไม่มีโค้ดใดเรียก |
| `inventory_counts` | 5,971 | ✅ SELECT+INSERT | นับ/บันทึกได้ แต่ **แก้ไข/ลบไม่ได้** → audit_check ทุกโหมดตาย, cycle_config ผูกรอบไม่ได้ |
| `inventory_audit_logs` | 717 | ✅ SELECT+INSERT | ปกติ |

**ทำไมเทส + smoke ครั้งแรกจับไม่ได้:**
1. เทสอัตโนมัติเป็น Dry Run (mock DB) — ตามนิยามจะไม่เห็นพฤติกรรม RLS ของ DB จริง
2. Smoke มือดู "badge เชื่อมต่อแล้ว" ซึ่งยิงไปที่ `inventory_counts` — เป็นตารางเดียวที่**มี** policy อยู่แล้ว จึงเขียวหลอกตา
→ **บทเรียน: เพิ่มขั้น "ตรวจว่าอ่านข้อมูลได้จริงทุกตารางหลัก" เข้า Smoke Checklist** (เพิ่มแล้วข้างล่าง)

**สถานะความเสี่ยงตอนพบ:** ยังไม่ได้ commit/push → ระบบที่ deploy อยู่ยังเป็นโค้ดเดิม ไม่มีผู้ใช้ได้รับผลกระทบ

**การแก้ (admin อนุมัติแล้ว 2026-08-09):** รัน `docs/sql/016_rls_policies.sql` — เพิ่ม RLS policy ให้ anon ตามที่แต่ละหน้าต้องใช้ **เพิ่ม policy อย่างเดียว ไม่แตะข้อมูลหรือโครงสร้างตาราง** ถอยได้ด้วย `DROP POLICY`

**ผลหลังแก้ (ยืนยันด้วย publishable key จริงในเบราว์เซอร์):**

| ตรวจ | ผล |
|---|---|
| อ่าน 10 ตารางหลัก | ✅ ครบทุกตาราง ตรงกับจำนวนแถวใน DB (5,326 / 5,628 / 5,971 / 1,179 …) |
| UPDATE/DELETE `inventory_counts`, `sku_master` | ✅ อนุญาต (ทดสอบด้วย id ที่ไม่มีจริง — ไม่แตะข้อมูล) |
| `index.html` | ✅ badge เขียว · dropdown รอบมีรอบจริง · progress 59% · ยังไม่ได้นับ 419 (ก่อนแก้เป็น 0/0%) |
| `reconcile.html` | ✅ 6 รอบใน dropdown · reconciliation_lines 1,088 แถวของรอบล่าสุด |
| `dashboard.html` | ✅ KPI ขึ้นครบ (นับแล้ว 628 · ยังไม่นับ 419 · 59%) |
| `node tests/run.mjs` | ✅ 57 PASS / 0 FAIL |

> หมายเหตุ: dropdown คลังแสดง 1 คลังถูกต้องแล้ว — registry มี 4 แต่ `is_active = true` แค่ `ตึกกันตนา`

---

## Impact Map — แก้ไฟล์นี้ กระทบหน้าไหนบ้าง

แก้ shared JS 1 ไฟล์ = ทุกหน้าในแถวนั้นต้องถูกทดสอบมือซ้ำ (อย่างน้อยตาม Smoke Checklist ข้างล่าง)

| ไฟล์ที่แก้ | หน้าที่ได้รับผลกระทบ |
|---|---|
| `Js/api.js` | **ทั้ง 11 หน้า** |
| `Js/sidebar-shared.js` | **ทั้ง 11 หน้า** (เมนู + ระบบแจ้งเตือนแชท) — แตะแล้วต้อง bump `ASSET_VER` + `?v=` ของ `sidebar-shared.js` **และ** `style.css` ทุกหน้า |
| `Js/reconcile-shared.js` | index, import_counts, count_search, reconcile, book_explorer, dashboard, live_count_wall, cycle_config |
| `Js/warehouses-shared.js` | **8 หน้า** — index, import_counts, count_search, audit_check, dashboard, live_count_wall, settings, cycle_config |
| `Js/settings-shared.js` | **8 หน้า** — index, import_counts, count_search, audit_check, reconcile, live_count_wall, settings, cycle_config (ยกเว้น book_explorer, chat, **dashboard** — dashboard มี connection pill ของตัวเอง) |
| `Js/sku-utils.js` | **8 หน้า** — index, import_counts, count_search, audit_check, reconcile, dashboard, live_count_wall, cycle_config |
| `Js/db-errors.js` | **3 หน้า** — index, import_counts, audit_check (ถอดออกจาก 5 หน้าเมื่อ 2026-08-10 เพราะไม่มีการเรียกใช้ · มีเทส `unit/script-loads` คุ้ม) |
| `Js/ui-confirm-modal.js` | **5 หน้า** — import_counts, audit_check, reconcile, cycle_config, chat |
| `Js/chat-notify-shared.js` | **ทั้ง 11 หน้า** (inject ผ่าน sidebar) |
| `Js/dashboard-shared.js` | dashboard |
| `Js/audit-dedupe.js` | audit_check |
| `Js/audit-log.js` | audit_check (และรูปแบบ log กระทบ drawer ประวัติใน index) |
| `Js/script.js` | index เท่านั้น |
| `Js/live-count-wall.js` | live_count_wall เท่านั้น |
| `Css/style.css` | **ทั้ง 11 หน้า** |
| `Css/ui-confirm.css` | **5 หน้า**ที่ใช้ uiConfirm — import_counts, audit_check, reconcile, cycle_config, chat |
| `docs/sql/*` (ถ้า admin อนุมัติในอนาคต) | ตาม RPC/ตารางที่แตะ — ปัจจุบัน **ห้ามแตะ DB** |

**ตัวอย่างการใช้:** จะแก้ H6 (`Js/reconcile-shared.js`) → แถว reconcile-shared บอกว่า 8 หน้าใช้ไฟล์นี้ → หลังแก้ รันเทสอัตโนมัติ + ไล่ smoke มือ 8 หน้า (โฟกัส reconcile ที่เรียกฟังก์ชันนี้ตรง ๆ 2 จุด: Excel import และ ลบรายการ Book)

---

## Smoke Checklist ทดสอบมือรายหน้า (หลังแก้ไฟล์ที่กระทบหน้านั้น)

**ขั้นที่ 0 (บังคับ — เพิ่มหลังเจอ Regression C1): ตรวจว่าอ่านข้อมูลได้จริงทุกตารางหลัก ไม่ใช่แค่ badge เขียว**
วางใน DevTools Console หน้าใดก็ได้ที่โหลด `api.js` — ทุกตารางต้องคืนจำนวนแถว **ไม่ใช่ 0**:

```js
(async () => { const c = window.apiService.getClient(); const out = {};
  for (const t of ['inventory_counts','warehouses','count_cycles','book_stock_lines','reconciliation_lines']) {
    const { data, error } = await c.from(t).select('*').limit(1);
    out[t] = error ? 'ERR: ' + error.message : (data.length ? 'อ่านได้' : '⚠️ ว่างเปล่า'); }
  console.table(out); })()
```

> ⚠️ RLS ปฏิเสธแบบ **เงียบ** — คืน HTTP 200 พร้อม 0 แถว ไม่ใช่ error สีแดง Console ที่สะอาดจึงไม่ได้แปลว่าระบบทำงาน

จากนั้นเปิดหน้าในเบราว์เซอร์ + เปิด DevTools Console (ต้องไม่มี error สีแดง) แล้วไล่ตามนี้ — **ระวัง: นี่คือระบบจริง ถ้าไม่อยากเขียนข้อมูลจริงให้ทดสอบเฉพาะส่วนอ่าน/แสดงผล หรือใช้คลัง+รอบทดสอบแยก**

| หน้า | เช็คขั้นต่ำ (อ่านอย่างเดียว — ปลอดภัย) |
|---|---|
| index | โหลดหน้า → KPI 3 ช่องขึ้นตัวเลข, dropdown รอบมีรายการ, autocomplete SKU เด้ง, รายการล่าสุดแสดง |
| import_counts | แถบรอบขึ้นสีถูก (เขียว/เหลือง), ลากไฟล์ template → preview ขึ้น (อย่ากดนำเข้า) |
| count_search | โหลดเดือนที่มีข้อมูล → ค้นหา → ตารางขึ้น + summary ตรง |
| audit_check | เลือกคลัง+เดือน → ตารางโหลด + สถิติสถานะขึ้น (อย่ากด apply/ลบ) |
| **audit_check** | ⚠️ **บังคับทุกครั้งที่แตะไฟล์นี้** — เลือกคลัง+เดือน → ตารางโหลด **และแถบสถิติต้องไม่เป็น 0 ทั้งแถว** (ถ้าเป็น 0 = pipeline ตรวจสอบตาย) · Console ต้องไม่มี error · เปิดโมดัลเทียบตำแหน่งแล้วกดยกเลิก |
| reconcile | เลือกรอบ → คำนวณ Match → KPI + ตารางขึ้น, ตัวเลข "ต่าง" สมเหตุผล (อย่ากดปรับยอด) |
| cycle_config | รายการรอบโหลด, เลือกรอบ → panel รายละเอียดขึ้น (อย่ากดลบ/นำเข้า) |
| book_explorer | เลือก ปี→เดือน→รอบ → ตาราง + KPI ขึ้น, ลอง sort + ค้นหา |
| dashboard | ทั้ง 2 แท็บโหลด, เปลี่ยนฟิลเตอร์คลัง/รอบแล้วตัวเลขเปลี่ยนสมเหตุผล |
| settings | badge เชื่อมต่อเขียว, รายการคลังโหลด (อย่ากดลบคลัง) |
| chat | ข้อความเก่าโหลด, ส่งข้อความทดสอบ 1 ข้อความได้ (ห้องรวม — ลบไม่ได้รายข้อความ) |
| live_count_wall | สองฝั่งโหลด, ลองสลับคลัง/รอบ, ดู realtime ขึ้นเมื่อมีคนบันทึก |

---

## บันทึกการแก้ไข (Change Log — เติมทุกครั้งที่แก้)

| วันที่ | Issue | ไฟล์ | ผลเทสก่อน/หลัง | Regression? | หมายเหตุ |
|---|---|---|---|---|---|
| 2026-08-09 | — (สร้างระบบเทส baseline) | tests/* (ไฟล์ใหม่ล้วน) | 50 PASS / 0 FAIL / 6 KNOWN-OPEN | — | baseline แรก — ยังไม่มีการแก้โค้ดระบบ |
| 2026-08-09 | C1 (เริ่ม) | `tests/unit/no-secret-keys.test.mjs` (ใหม่), `.gitignore` (ใหม่) | 51 PASS / 0 FAIL / 7 KNOWN-OPEN | — | เพิ่มเทสสแกน JWT + .gitignore — รอ anon key จาก admin ก่อนแก้ `Js/api.js` (โปรเจกต์ nfhfuybqhskzlllkgmyi ไม่อยู่ในบัญชี MCP ที่เชื่อม) |
| 2026-08-09 | C1 (ฝั่งโค้ดเสร็จ) | `Js/api.js` (+31/-1) | ก่อน: 50/0/6 → หลัง: 55/0/6, C1 เด้ง KNOWN-FIXED แล้วย้ายเป็นยามถาวร | ไม่มี | เปลี่ยนเป็น `sb_publishable_...` + เพิ่ม `isServiceRoleKey()` ล้าง key admin ค้างใน localStorage อัตโนมัติ — **ค้าง: admin ต้อง revoke service_role เก่าใน dashboard** (key ยังอยู่ใน git history) |
| 2026-08-09 | C1 (เก็บงานตาม review) | `Js/api.js` (export isServiceRoleKey + try/catch seed + warn), `Js/settings-shared.js` (+2), `Html/settings.html` (+4) | 57/0/6 | ไม่มี | ตามข้อเสนอ code-reviewer: block service_role ตั้งแต่ตอนบันทึกใน settings + แก้ pre-existing setItem ไม่มี try/catch — smoke ครั้งแรก "ผ่าน" (badge เขียว) แต่**ไม่เพียงพอ** ดูแถวถัดไป |
| 2026-08-09 | **C1 — พบ REGRESSION** (หลังเชื่อม Supabase MCP แล้วตรวจ RLS จริง) | — | 57/0/6 (เทส dry-run ยังเขียว — จับไม่ได้ตามนิยาม) | ⚠️ **ใช่ — 7 ตารางคืนค่าว่าง** | anon ไม่ข้าม RLS แต่ 7 ตารางไม่มี policy → ระบบใช้งานไม่ได้จริง · ยังไม่ push → production ปลอดภัย · เพิ่มขั้นที่ 0 เข้า Smoke Checklist |
| 2026-08-09 | **H5 — เสร็จ** | `Html/reconcile.html` (change listener + `invalidateCycleView` + `lockCycleId`/`stillOnCycle` + ล็อก dropdown + ป้าย "ผลคำนวณเดิม"), `Js/reconcile-shared.js` (export `isoToBangkokYmd`), เทสใหม่ `reconcile-cycle-sync` | ก่อน: 123/0/5 → หลัง: **133/0/5** | ไม่มี | รากของบั๊กคือ **อ่าน `currentCycle.id` ตอนเขียน ไม่ใช่ตอน guard** — review ชี้จุดนี้ทำให้แก้ได้ตรงจุด · review ยังพบว่าปุ่มลบ draft หลุด guard ทั้งหมด (inline handler) และเทสรุ่นแรกที่ใช้รายชื่อ hardcode มองไม่เห็น → เปลี่ยนเป็นสแกน call site |
| 2026-08-09 | **H4 + H9 — เสร็จ** | `Js/dashboard-shared.js` (+`computeBookCoverage`), `Html/dashboard.html` (เลิก loop ต่อคลัง + ลบ logic ซ้ำใน render), `Js/reconcile-shared.js` / `Js/script.js` / `Js/live-count-wall.js` / 3 HTML (+`.order('id')` 10 จุด), เทสใหม่ `stable-paging` | ก่อน: 113/0/5 → หลัง: **123/0/5** | ไม่มี | **รอบ TIKTOK: Book 2,902→1,451 · ยังไม่นับ 827→49** ตรง DB เป๊ะ · review จับได้ว่า guard ตัวแรกของผม**ตาบอดต่อโค้ดที่เพิ่งแก้เอง** (regex หยุดที่ `.range()` ในคอมเมนต์) แก้แล้วเผยอีก 10 จุด · presence map ใน reconcile เสถียรแล้ว (1,503 เท่ากันทุกครั้ง) |
| 2026-08-09 | **H3 — เสร็จ (ส่วน audit log)** | `Js/audit-log.js` (ใหม่), `Html/audit_check.html` (ต่อ log ครบ 5 จุด + flush ทุก 100 แถว + `dataset.originalSku/Qty`), `Js/script.js` (ป้ายกำกับ `AUDIT_*`/`IMPORT` ใน drawer) | ก่อน: 92/0/5 → หลัง: **113/0/5** | ไม่มี | review จับได้ 4 จุดสำคัญที่แก้ตาม: log เขียนครั้งเดียวหลัง loop (ปิดแท็บ = ไม่มี log เลย), chunk พังแล้วเขียนต่อจนเหลือหลักฐานเท็จ, `Number('')===0` ทำให้บันทึก "จำนวน 0" ผิด, snapshot อ่านค่าที่ผู้ใช้พิมพ์ค้างแทนค่าใน DB · ส่วน atomic แยกเป็น M27 |
| 2026-08-09 | **H2 — เสร็จ** | `Js/audit-dedupe.js` (ใหม่ — นิยาม "แถวซ้ำ" + เทส 19 ข้อ), `Html/audit_check.html` (ใช้โมดูลใหม่, ดึง cycle_id/counter_name/import_batch_id, สำรอง CSV ก่อนลบ, verify error→warn, classify เป็น post-pass) | ก่อน: 72/0/5 → หลัง: **92/0/5** | ไม่มี | **ผลจริง 6,078 แถว: กฎเก่าจะลบ 470 แถว → กฎใหม่ลบ 0** · review รอบแรกจับได้ว่าผมตีความ `created_at` ตรงกันเป๊ะผิด (เป็น bulk insert ไม่ใช่กดซ้ำ) แก้แล้วพร้อมเทส · พบ M26 เป็นข้อใหม่ |
| 2026-08-09 | **C2 (XSS) + M25 (client cache) — เสร็จ** | `Js/script.js`, `Js/api.js`, `Js/sidebar-shared.js` (ASSET_VER + cache-buster ให้ script ที่ inject), escape function 14 ตัวทุกไฟล์, `Html/` settings·audit_check·sku_master·import_counts·count_search·book_explorer·dashboard, เทสใหม่ `xss-guard` + `api-client-cache` | ก่อน: 63/0/5 → หลัง: **72/0/5** | ไม่มี — ทดสอบจริง 6 หน้า | เปลี่ยน onclick ที่ต่อค่าเข้า JS string → `data-*` + `this.dataset` · review เจอ sink ที่ ISSUES เดิมตกหล่น 5 จุด (mini-dashboard ใน index, modal ลบขั้น 2, ตัวกรองผู้นับ) เก็บครบแล้ว · แก้ `highlightMatch` ให้ไฮไลต์ apostrophe ได้ · cache-buster → `20260809c` |
| 2026-08-09 | **H1 — เสร็จ** | `Js/reconcile-shared.js` (+`isCycleRelevantNow`,`bangkokYearMonthNow`), `Js/script.js` (+`ensureCycleStillValid` เรียกใน 2 submit path), `Js/sidebar-shared.js` (prefix-match src), `Html/import_counts.html` (ข้อความ banner), **HTML ทุกไฟล์** (cache-buster `?v=20260809a` 30 tags) | ก่อน: 57/0/6 → หลัง: **63/0/5** (H1 ย้ายจาก knownIssue → เทสถาวร 6 ข้อ) | ไม่มี — ทดสอบจริงครบ 8 หน้า | **ค้นพบระหว่างทาง:** เบราว์เซอร์ cache `reconcile-shared.js` ไว้ ทำให้การแก้ไม่มีผล → ต้องใส่ cache-buster ถึงจะ deploy ได้จริง · review เจอ M24 (รอบ closed ยังรับผลนับ) บันทึกเป็นข้อใหม่ |
| 2026-08-09 | **C1 — revoke key เก่า (admin ทำใน dashboard)** | — (ฝั่ง Supabase) | 57/0/6 | ไม่มี | legacy `anon`+`service_role` = `disabled: true` · GitHub alert #1 = `resolved/revoked` · ยืนยันหน้า index ยังปกติ (59% / 419) และอ่านครบ 10 ตาราง · **C1 ปิดสมบูรณ์** |
| 2026-08-09 | **DB cleanup** (admin อนุมัติ) | `docs/sql/017_drop_skunorm_backup_tables.sql`, `docs/backup/2026-08-09_bk_tables_unique_rows.md` | 57/0/6 | ไม่มี — ทุกตารางที่ระบบใช้ยังอ่านได้ครบ | ลบตารางสำรอง `_bk_*` 3 ตัว (5,225 แถว) หลังตรวจพบว่า 5,222 แถวซ้ำกับตารางจริง 100% · สำรอง 3 แถวที่ไม่ซ้ำเป็น CSV+SQL ไว้แล้ว |
| 2026-08-09 | **C1 — ปิดงาน** (admin อนุมัติเพิ่ม RLS policy) | `docs/sql/016_rls_policies.sql` (รันจริงผ่าน MCP: 11 policies บน 9 ตาราง) | 57 PASS / 0 FAIL / 6 KNOWN-OPEN | ✅ **regression หายแล้ว** | ทุกตารางอ่าน/เขียนได้ครบ · ทดสอบหน้า index/reconcile/dashboard จริงผ่าน · **ค้าง: admin revoke service_role key เก่าใน dashboard** |
| 2026-08-09 | **H6 + H7 — เสร็จ** (ทำขนานกับ H5 คนละไฟล์) | `Js/reconcile-shared.js` (+`importBookAndRecompute`, `logAdjustmentsBeforeDelete`, `clearAdjustments…` คืนจำนวน, `deleteBookStockBySku` log ก่อนลบ), `Html/reconcile.html` (call site import + ลบกลไก cache accept), `Js/dashboard-shared.js` (ตัวหาร avgPerMin), เทส `dryrun/clear-adjustments` + `unit/dashboard-shared` | ก่อน: 130/0/5 → หลัง: **156 PASS / 0 FAIL / 3 KNOWN-OPEN** (ในนั้นเป็นเทส H8 ของอีกเซสชันที่อยู่ใน working tree เดียวกัน 11 ข้อ — เฉพาะขอบเขต H6+H7 คือ 145) | ไม่มี | admin เลือก semantic: import = "Book ใหม่คือความจริง" → เลิก force ทุก SKU เป็น match, ปล่อยให้ `refresh_reconciliation_for_cycle` ตัดสิน · ยอดปรับเก่ายัง**ต้อง**ลบทั้ง draft/applied (ไม่งั้น `effective = book + SUM(applied)` นับซ้ำ) แต่เขียน `inventory_audit_logs` (`RECONCILE_ADJ_CLEAR`) ก่อนลบ ถ้า log ไม่สำเร็จ = ยกเลิกการลบ · พบเส้นทางที่สองที่ลบ adjustment โดยไม่ log: `deleteBookStockBySku` แก้ด้วย (ยังเหลือ `deleteCycle` → M29) · H7 ใช้ช่วง bucket แรก→สุดท้าย ไม่ใช่ max−min (max−min สั้นกว่าจริง 1 bucket) · **review จับได้ 2 ข้อ High เรื่องลำดับ operation**: (1) เดิม merge Book ก่อนล้างยอดปรับ ถ้าล้างพังจะได้ Book ใหม่ + ปรับเก่า = นับซ้ำเงียบ ๆ → สลับเป็นล้างก่อน merge (2) `deleteBookStockBySku` ลบ Book ก่อน log → ย้าย log ขึ้นก่อน + เพิ่ม `rollbackAuditEntries` เมื่อการลบพังหลังเขียน log แล้ว · cache-buster → `20260809k` เฉพาะ 8 หน้าที่โหลดไฟล์ที่แก้ |
| 2026-08-09 | **C1 — regression ตกค้างรอบ 2: "คำนวณ Match" 401** | `docs/sql/018_refresh_reconciliation_security_definer.sql` (รันจริงผ่าน MCP: `ALTER FUNCTION` 2 บรรทัด) | 108 PASS / 0 FAIL / 5 KNOWN-OPEN (ก่อน=หลัง — เทส dry-run จับ RLS ไม่ได้ตามนิยาม) | ✅ **แก้แล้ว** | 016 ให้ `reconciliation_lines` แค่ policy SELECT เพราะคิดว่า RPC เขียนได้อยู่แล้ว แต่ `prosecdef=false` → RPC รันด้วยสิทธิ์ anon → `DELETE` ลบ 0 แถวเงียบ ๆ + `INSERT` โดน 42501 → PostgREST คืน **HTTP 401** · reproduce ด้วย `set local role anon` + rollback ยืนยันทั้งก่อน (42501) และหลัง (00000, 1,113 แถว) · `apply_stock_adjustment` / `apply_all_drafts_for_cycle` หายตามเพราะ PERFORM ต่อเข้าฟังก์ชันนี้ |
| 2026-08-09 | **H8 — เสร็จ** (ทำขนานกับงานอีกฝั่ง — footprint คือ `Html/chat.html` ไฟล์เดียว) | `Html/chat.html` (+`#clearConfirmModal`, `runClearNow`, `fetchAllRoomMessages`, `downloadChatJson`, `writeClearTrace`, `statusHoldUntil`), เทสใหม่ `unit/chat-clear-guard` | ก่อน: 145/0/3 → หลัง: **161 PASS / 0 FAIL / 3 KNOWN-OPEN** | ไม่มี | แก้ครบ 3 อาการ: `okLabel`→`confirmLabel` (ทั้งโปรเจกต์เหลือ 0 จุด), เพิ่ม else โหมด local (เดิมค้าง "กำลังล้างแชท..." ตลอดไป), และเพิ่ม 3 ด่านแทน authorization ที่ระบบไม่มี: ต้องระบุชื่อผู้ทำ → สำรองอัตโนมัติ (สำรองพัง = ไม่ลบ) → พิมพ์คำว่า "ล้างแชท" · เจตนาไม่แตะ `Js/ui-confirm-modal.js` เพื่อเลี่ยง bump cache-buster 6 ไฟล์ HTML ที่อาจชนกับงาน H6/H7 · **review จับได้ 6 ข้อที่แก้ตาม**: (1) realtime echo ของข้อความร่องรอยเรียก `updateHint` ทับคำเตือน RLS หายใน <1 วิ → เพิ่ม `statusHoldUntil` (2) paging ของการสำรองเลื่อน cursor ตายตัว ถ้า PostgREST คืนไม่ครบหน้าจะข้ามแถวแล้วเลิกลูปเงียบ ๆ → เลื่อนตาม `data.length` + เทียบ `count:'exact'` (3) `storage.remove()` ที่โดน policy บล็อกคืน `data: []` โดยไม่ error → ต้องอ่าน `data` ไม่ใช่แค่ `error` (4) เทียบ "ลบ 0 แถว" กับ mirror ในเครื่องซึ่งอาจว่างอยู่แล้ว → ใช้จำนวนที่อ่านจากเซิร์ฟเวอร์ตอนสำรอง (5) กดปุ่มล้างซ้ำระหว่างด่าน 3 = โปรมิสเดิมค้างถาวร → `clearInProgress` + settle ตัวเก่า (6) เทส 3 ข้อผ่านแบบหลอก (ลืม `await`, ปุ่มยกเลิก resolve(true), ลูป Storage รอบเดียว) → รัดเทสใหม่จน mutation ทั้ง 7 แบบถูกจับครบ · ทดสอบจริงบนห้อง `main` 20 ข้อความ: ไฟล์สำรองลงเครื่องครบ 20 แถวจากเซิร์ฟเวอร์, กดยกเลิกแล้วไม่มีอะไรถูกลบ (DB ยัง 20 แถว `created_at` เดิม), โหมด local รายงานถูกต้องไม่ค้าง |
| 2026-08-10 | **UI1 + M22 — เสร็จ** (เมนูซ้ายบนจอเล็ก) | `Css/style.css` (แทนที่ `@media 900px` ทั้งบล็อก + `.sidebar-mobile-bar`/`.sidebar-toggle`/`.sidebar-scrim` + `--sidebar-bar-h` + `overflow-y`/`100dvh` ที่ `.sidebar`), `Js/sidebar-shared.js` (+`renderMobileChrome`/`setDrawer`/`pageLabel` + matchMedia + Escape), `Css/chat-notify.css` (ดัน toast ลงใต้แถบ), `Html/book_explorer.html` (`has-sidebar` + padding + sticky top), **13 HTML** (cache-buster `20260810a` ให้ `style.css`+`sidebar-shared.js`+`ui-confirm.css`), เทสใหม่ `unit/sidebar-responsive` | ก่อน: 171/0/3 → หลัง: **183 PASS / 0 FAIL / 3 KNOWN-OPEN** (ในนั้นมีงาน H10/H8 ของเซสชันอื่นใน working tree เดียวกัน) | ไม่มี | รากของบั๊ก: `@media (max-width:900px)` เขียนสมัยเมนูเป็น "แถวไอคอนแบน" แต่ JS เปลี่ยนไปเรนเดอร์ "กลุ่มพับได้" แล้ว — CSS สั่ง `flex-direction: row` ทับ DOM แนวตั้ง · เปลี่ยนเป็น off-canvas drawer + ☰ + ฉากมืด · **review จับได้ 1 High ที่ผมพลาดจริง**: ผมอ่าน "style.css:756 .toast-container" จากผลสำรวจแล้วเข้าใจว่า 756 คือ z-index ทั้งที่เป็น**เลขบรรทัด** ค่าจริงคือ 50 → ตั้งแถบเมนูไว้ 720 ทับ `.log-drawer` (210) จนปุ่มปิดกดไม่ได้บนมือถือ แก้เป็น 30/35/40 (ต่ำกว่า overlay ทุกตัว) + เพิ่มเทสเทียบ z-index อัตโนมัติ · อีก 3 ข้อที่แก้ตาม: ลิ้นชัก `top:0` ทำให้หัวเมนูถูกแถบทับ (→ `top: var(--sidebar-bar-h)`), chat toast กลืน tap ปุ่ม ☰ บนจอ <392px, และเทสรุ่นแรกมีทั้ง false-FAIL (regex `class="([^"]+)"` มองไม่เห็น class ที่ต่อสตริง) และ false-PASS (อ่านบล็อก 900px แค่ก้อนแรก) — รัดใหม่แล้วพิสูจน์ด้วย mutation 2 แบบว่าจับได้จริง · พบว่า `style.css` และ `ui-confirm.css` **ไม่เคยมี `?v=` เลย** → แก้ CSS ที่ผ่านมาไม่เคยถึงผู้ใช้ที่เปิดระบบมาก่อน เพิ่มเทสยามกว้าง "ทุกไฟล์ Js/Css ต้องมี `?v=`" |
| 2026-08-10 | **Dead code sweep ชุด 1-5 — เสร็จ** (สแกนทั้งระบบ 3 มุม: JS / DB / asset+CSS+docs) | **ลบ**: asset กำพร้า 4 ไฟล์ (~4.0 MB, `assets/` 8.0→4.0 MB) · `tests/helpers/harness.mjs` (`clearRegistry`) · `<script src>` ที่โหลดฟรี 6 จุด (`db-errors.js` × 5 หน้า, `settings-shared.js` × dashboard) · ฟังก์ชัน/id/CSS ตายรายหน้า 19 จุดใน 8 HTML · `Js/script.js` ฟีเจอร์ Extra SKU Drawer ทั้งชุด (−205 บรรทัด) — **เพิ่ม**: เทสใหม่ `unit/script-loads` (5 ข้อ) — **แก้เอกสาร**: SYSTEM_GUIDE, RECONCILIATION_DESIGN, DATABASE (+หัวข้อ "DB objects ที่ไม่มีโค้ดใช้"), ARCHITECTURE (+matrix การโหลดใหม่ทั้งตาราง), `pages/` 6 ไฟล์, ISSUES L1/L2/L8 | ก่อน: 244/0/3 → หลัง: **249 PASS / 0 FAIL / 3 KNOWN-OPEN** | ไม่มี | คำถามตั้งต้นคือ "SKU Master ผูกกับอะไรบ้าง" — คำตอบ: **หน้าเดียว** (`reconcile.html` เรียก `fetchSkuMasterNamesBySkus` ตอนกด "สร้างลง Book" และมี fallback อยู่แล้ว) ส่วน `index.html` grep คำว่า "master" = **0 hit** ใช้ `book_stock_lines` ล้วน → เอกสาร 4 ไฟล์ที่บอกว่า KPI อิง SKU Master ผิดมาตลอด · `buildExtraCountedItems` ไม่ใช่แค่โค้ดตายเฉย ๆ — `updateStats()` เรียกมันทุกครั้งที่บันทึกผลนับ 1 แถว วน Map+Set ทั้ง 6,000 แถวแล้วส่งผลไปเข้า 3 สาขาที่ element ไม่มีจริง · **review จับได้ 5 ข้อที่แก้ตาม**: (1) เทสใหม่รุ่นแรกมี false negative — regex บังคับต้องมี `.`/`?.`/`[` ตามหลัง จึงมองไม่เห็น pattern `const RS = window.reconcileService` ที่ระบบใช้จริง และมองไม่เห็น global ที่เป็นฟังก์ชันเดี่ยว (`checkSupabaseConnection()`) ทั้งหมด → เปลี่ยนเป็น `(window\.)?NAME` + เพิ่ม PROVIDERS จาก 7 เป็น 18 ตัว + เพิ่มเทสที่สแกน `window.X =` ในไฟล์จริงมาบังคับว่าตาราง PROVIDERS ห้ามตกหล่น (เทสตัวนี้จับ `===` เป็นการประกาศทันทีที่เขียน — แก้ด้วย `=(?!=)`) พิสูจน์ด้วย mutation แล้วว่าจับ 2 pattern ที่เคยหลุด (2) `tests/run.mjs` พิมพ์แค่บรรทัดแรกของ error → รายชื่อหน้าที่ผิดหลัง `
` ไม่เคยโผล่ เปลี่ยนตัวคั่นเป็น ` | ` (3) matrix การโหลด shared JS ใน ARCHITECTURE ล้าสมัยทันทีจากคอมมิตตัวเอง (4) Impact Map ใน FIX_TRACKING เองก็ผิด — ถ้าไม่แก้ ครั้งหน้าที่แก้ `db-errors.js` จะไป bump `?v=` ของ tag ที่ไม่มีอยู่แล้ว (5) `docs/pages/` 5 ไฟล์ยังบอกว่าโหลด db-errors · **ยังไม่ทำ (ชุด 6-9)**: `reconcile-shared.js` 6 ฟังก์ชัน ~180 บรรทัด, `ui-confirm-modal`/`live-count-wall`, dead handler ใน `audit_check.html`, บล็อก CSS ตาย ~200 บรรทัดใน `style.css` · **ห้ามทำ**: ลบ `@import` ฟอนต์ Outfit (มี `<link>` แค่ 4/13 หน้า แต่ CSS ใช้ 12 จุด) และตัด export ~27 key ที่มีแต่เทสใช้ (`window.x` คือ test surface เดียวที่ระบบไม่มี bundler มี) |
| 2026-08-10 | **ถอดฟีเจอร์ SKU Master ออกจากเว็บ — เสร็จ** (มติ admin: ตัดการเชื่อมต่อฝั่งเว็บ **ห้ามลบตารางออกจาก Supabase**) | **เฟส 1** `Js/reconcile-shared.js` (ลบ `fetchSkuMasterNamesBySkus` → `fetchBookNamesBySkusAnyCycle` อ่าน `book_stock_lines` ข้ามรอบ), `Html/reconcile.html` (2 call site + ย้าย lookup มาก่อนโมดัล), cache-buster `20260810h` 8 หน้า, เทสใหม่ `dryrun/book-names` × 8, แก้ mock `.order()` ให้เป็น compound · **เฟส 2** ลบ `Html/sku_master.html` (1,135 บรรทัด) + `docs/pages/sku_master.md` + `template_Import/`, `Js/sidebar-shared.js` (ลบทั้งกลุ่ม `database` + `PAGE_FILES` + `FLAT_PAGES` + `ASSET_VER`→`20260810h`), 12 HTML bump `sidebar-shared.js`+`style.css`, `Html/settings.html` ข้อความ badge, `tests/unit/xss-guard.test.mjs` (ลบ entry + `existsSync` guard), เทสใหม่ `[menu-guard]` × 4 · **เฟส 3** เอกสาร 9 ไฟล์ + `Html/user_manual.html` (ลบ section F1 + TOC + 2 ตาราง + ER) + `assets/manual/dfd-level1.svg` (ลบกล่อง P1/D1 + ลูกศร) | ก่อน: 249/0/3 → หลัง: **264 PASS / 0 FAIL / 3 KNOWN-OPEN** | ไม่มี | **จุดที่เกือบพลาด:** `reconcile.html` เขียน `namePro: masterNames[sku] || skuNameMap[sku] || null` ดูเหมือนมี fallback แต่ **ตายสนิท** — `canAddToBookLine` บังคับ `!bookSkuSet.has(sku)` และทั้ง `skuNameMap`/`bookSkuSet` มาจาก `book_stock_lines` ของ cycle เดียวกัน ⇒ ตัด lookup ทิ้งเฉย ๆ = `name_pro` เป็น null **100%** เขียนลง DB ถาวร และไม่มี UI แก้รายแถว · แหล่งใหม่ (`book_stock_lines` ข้ามรอบ) กว้างกว่าเดิมและปิดบั๊กเดิมที่ query โดยไม่กรอง `warehouse` ทั้งที่ตารางแยกต่อคลัง · **กลุ่ม `database` ต้องลบทั้งกลุ่ม** ไม่ใช่แค่ item — `sidebar-shared.js:149` special-case `items.length === 1` เรนเดอร์เป็นลิงก์แบน ถ้าเหลือ 0 จะตกไปสาขากลุ่มพับ = หัวข้อ "ฐานข้อมูล" ที่กดแล้วว่างทั้ง 12 หน้า (เพิ่มเทส `[menu-guard]` + พิสูจน์ด้วย mutation แล้ว) · `xss-guard.test.mjs` hardcode ชื่อไฟล์แล้ว `readFileSync` ไม่มี existsSync → ลบไฟล์แล้วได้ FAIL ที่ข้อความเป็น ENOENT · **แก้ mock ระหว่างทาง:** `_applyFilters` วน `.order()` ทีละตัว ตัวหลังล้างผลตัวแรก — ตรงข้ามกับ PostgREST จริง ทำให้เทสที่พึ่ง tiebreak ตาม invariant ข้อ 13 ผ่านแบบหลอก · **review จับได้ 5 ข้อที่แก้ตาม**: (1) เทส dead-fallback ที่เขียนไว้ **ใช้งานไม่ได้จริง** — regex ใช้ `[\w.]*` ก่อน `||` ซึ่งไม่ครอบ `[` `]` จึงจับ `masterNames[sku] || skuNameMap[sku]` ที่เป็นรูปแบบจริงในโค้ดไม่ได้เลย (mutation ใส่ fallback กลับเข้าไปแล้วยัง 261 PASS ฉลุย) → เปลี่ยนเป็น `namePro:[^\n;]*skuNameMap` แล้วพิสูจน์ด้วย mutation ใหม่ว่าจับได้จริง (2) query ใหม่ไม่แบ่งหน้า — SKU ที่กดปุ่มได้คือ SKU ที่อยู่ในรอบเก่า ซึ่งตรงกับส่วนที่ max-rows 1,000 จะตัดทิ้งพอดี → เพิ่ม `.range()` + `.order(id)` เป็น tiebreak + ลด chunk 100→25 + หยุดทันทีที่ได้ชื่อครบ + เทส 3 ข้อ (3) คอมเมนต์อ้างเกินจริงว่า "ผูกกับรอบซึ่งผูกคลังอยู่แล้ว" ทั้งที่ `book_stock_lines` ไม่มีคอลัมน์ warehouse และ query ไม่ join → แก้เป็น "จงใจไม่กรองคลัง เพราะชื่อสินค้าไม่ขึ้นกับคลัง" (4) ลบ section F1 แล้วเผลอลบ `<hr class="manual-divider">` ไปทั้งตัวหน้าและตัวหลัง (5) ⚠️ **ชื่อสินค้ากลายเป็น ALL CAPS** — แหล่งเดิม (`sku_master`) เก็บชื่อโดยคงตัวพิมพ์ แต่ `book_stock_lines.name_pro` ถูกเขียนผ่าน `normalizeSku` (= ISSUES M4 ที่ยังเปิดอยู่) ⇒ บันทึกไว้ใน M4 ว่าขอบเขตกว้างขึ้นแล้ว **ยังไม่แก้ในรอบนี้** (แก้ที่ `reconcile-shared.js:1141` บรรทัดเดียวจะปิดทั้ง 2 เส้นทาง) · **ยังต้องทำมือ:** `manual-editor.js` restore `innerHTML` จาก localStorage ทับ source ⇒ เครื่องที่เคยเข้าโหมดแก้ไขคู่มือต้องกด "สำรอง" แล้ว "รีเซ็ตคู่มือ" ถึงจะไม่เห็น section F1 |
| 2026-08-10 | **M4 — เสร็จ** (ชื่อสินค้าโดน UPPERCASE) | `Js/reconcile-shared.js` `parseBookExcelRows`: `normalizeSku(...)` → `String(...).trim()` เฉพาะคอลัมน์ชื่อ · `tests/unit/reconcile-book.test.mjs` ย้าย `knownIssue('M4')` → `test()` และรัดเพิ่มเป็น 3 ข้อ · cache-buster `20260810j` 8 หน้า | ก่อน: 264/0/3 → หลัง: **267 PASS / 0 FAIL / 2 KNOWN-OPEN** (M4 ออกจากรายการ known-open) | ไม่มี | แก้ต่อจากที่ code-reviewer จับได้ในงานถอด SKU Master — เดิมบั๊กนี้กระทบเฉพาะชื่อจากไฟล์ Book แต่พอ `sku_master` (ที่เก็บชื่อคงตัวพิมพ์) ถูกถอดออก ปุ่ม "สร้างลง Book" ก็ดึงชื่อจาก `book_stock_lines` บั๊กจึงจะไหลไปทุกเส้นทาง · invariant ข้อ 2 พูดถึง **รหัส SKU** เท่านั้น การเอา `normalizeSku` ไปใช้กับชื่อ เป็นการตีความเกิน — เพิ่มเทสข้อที่ 3 บังคับว่าคอลัมน์ SKU ต้องยัง UPPERCASE กันแก้เพลินถอดออกทั้งคู่ · ⚠️ **แถวที่นำเข้าไปแล้วยังเป็น ALL CAPS** แก้โค้ดไม่ย้อนไปแก้ข้อมูลเก่า ถ้าจะล้างต้องอัปโหลด Book ทับหรือ UPDATE ใน DB |
| 2026-08-11 | **ถอด checkbox "ทุกช่วงเวลา (รวมรอบเก่า)" ใน audit_check — เสร็จ** (ขั้น 1 ตาม handoff note · admin ระบุว่าแทบไม่ได้ใช้) | `Html/audit_check.html` จุดเดียว 18 แก้/69 ลบ: markup checkbox + CSS `.audit-data-scope input[type=checkbox]` + `getAuditFilters` (เอา `allTime` ออกจากสัญญาฟังก์ชัน) + `isLooseAuditScope` + `refreshAuditMonthSelect` (early-return) + `formatAuditScopeLabel` + `updateAuditScopeHint` + `saveAuditFilters` + `initAuditFilters` + ด่านตรวจ loc-compare 3 จุด + คอลัมน์เดือนใน Excel export 3 จุด + `onAuditAllTimeChange`/listener/element cache · เอกสาร `ARCHITECTURE.md`, `docs/pages/audit_check.md` | ก่อน: 267/0/2 → หลัง: **267 PASS / 0 FAIL / 2 KNOWN-OPEN** (ไม่มีเทสไหนอ้าง checkbox นี้) | ไม่มี | 🔴 **กับดักที่ handoff note เตือนไว้และหลบได้จริง**: `initAuditFilters()` มี guard `if (!warehouseEl || !monthEl || !allTimeEl) return;` — ถ้าลบ element เฉย ๆ ฟังก์ชันจะ **return ทั้งก้อนแบบเงียบ ไม่มี error** ⇒ `warehouseService.populateSelect()` ไม่ถูกเรียก dropdown คลังเหลือแต่ 3 option hardcode ใน HTML ไม่ sync registry · แก้ guard พร้อมกันแล้ว · **พิสูจน์ในเบราว์เซอร์**: dropdown คลังขึ้น `[ทุกคลัง, ตึกกันตนา]` = มาจาก registry จริง (ไม่ใช่ 3 ค่า hardcode), `getAuditFilters()` ไม่มีคีย์ `allTime` แล้ว, เลือกคลัง → auto pipeline โหลด 1,131 แถว + verify ครบ, console สะอาด · พ่วงลบ `const savedMonth` ที่อ่าน localStorage แล้วทิ้ง (พบตอน dead-code sweep แต่ข้ามไปเพราะตอนนั้นห้ามแตะไฟล์นี้) · localStorage `audit_check_all_time` เลิกเขียนแล้ว key เก่าค้างในเบราว์เซอร์ผู้ใช้ได้แต่ไม่มีผล |
| 2026-08-11 | **ถอดหน้าคู่มือ (user_manual) — เสร็จ** (ขั้น 2a ตาม handoff note · มติ admin) | ลบ `Html/user_manual.html`, `Js/manual-editor.js`, `docs/pages/user_manual.md`, `assets/manual/` 25 ไฟล์ + `assets/manual-ui-*.png` 2 ไฟล์ (**`assets/` 4.0 MB → 16 KB**) · `Js/sidebar-shared.js` (GROUPS + PAGE_FILES + `ASSET_VER`→`20260811a`) · 11 HTML bump `sidebar-shared.js`+`style.css` · ลบ entry `PAGE_JS` ใน `script-loads.test.mjs` · เอกสาร 8 ไฟล์ (12→11 หน้า) + ปิด ISSUES M20 + note self-XSS ใน C2 + ตัด 2 รายการจาก L4 | 267 PASS / 0 FAIL / 2 KNOWN-OPEN (เท่าเดิม) | ไม่มี | กลุ่มเมนู "ตั้งค่า" เหลือ 3 รายการ (จาก 4) จึงยังเป็นกลุ่มพับปกติ ไม่ตกไปโดน special-case `items.length === 1` ที่ `sidebar-shared.js:138` — เทส `[menu-guard]` × 4 ที่เพิ่มตอนถอด SKU Master คุมเคสนี้อยู่แล้ว · **ห้ามลบ `Css/ui-confirm.css`** อีก 5 หน้าใช้ (import_counts, audit_check, reconcile, cycle_config, chat) · localStorage `stock_audit_user_manual_v2` / `stock_audit_manual_mode_v1` จะค้างในเบราว์เซอร์ผู้ใช้ แต่ไม่มีโค้ดอ่านแล้ว — และเพราะหน้าถูกลบ ปุ่ม "รีเซ็ตคู่มือ" ก็ใช้ไม่ได้อีก (ยอมรับได้) · smoke: เมนู 3 กลุ่มไม่มีกลุ่มว่าง, ไม่มีคำว่า "คู่มือ", ลิงก์ 11 อันชี้ไฟล์ที่มีจริง, ไม่มี 404 |
| 2026-08-11 | **Dead code ชุด 6-9 — เสร็จ** (ปิด ISSUES L1/L2 ครบ) | `Js/reconcile-shared.js` ลบ 6 ฟังก์ชันตายสนิท ~193 บรรทัด (`applyAdjustmentsToBookTargets` 84 · `acceptReconciliationAsMatchBatch` · `fetchBookQtySums` · `updateCycleStatus` · `isMultiWarehouseCycle` · `importBookStockLinesMerge`) · `Js/ui-confirm-modal.js` (`parseMessage`) · `Js/live-count-wall.js` (`bangkokTodayRange`, `skuMatchesScope`, `window.liveCountWall`) · `Html/audit_check.html` (binding ปุ่ม `btnVerifyAll`/`btnLoadCounts` ที่ไม่มีใน markup + `skuLocKey` + `countDataRowsInZone` + `.audit-toolbar-left` + id `historySubtitle` + localStorage `audit_check_warehouse` ที่เขียนอย่างเดียว) · `Css/style.css` **−208 บรรทัด** (บล็อก KPI 20 คลาส + เศษใน media 2 จุด + `.sidebar-*-flat` + `--input-focus`) · เทส: `shared-smoke` เกณฑ์ `>= 80` → `>= 75` (export 85→79) และถอด 2 ชื่อออกจาก `RS_WRITE_FNS` · `ASSET_VER` `20260811b` + 11 หน้า | ก่อน: 267/0/2 → หลัง: **267 PASS / 0 FAIL / 2 KNOWN-OPEN** | ไม่มี | ⛔ **ห้ามลบ `@import` ฟอนต์ Outfit** — พิสูจน์ในเบราว์เซอร์แล้วว่า `reconcile.html` ไม่มี `<link>` Outfit แต่ฟอนต์ยังมาถึงผ่าน `@import` (`document.fonts` เห็นทั้ง Kanit และ Outfit) · **เก็บไว้โดยตั้งใจ**: `previewAdjustmentsToBookTargets` (ยังมีผู้เรียก 2 จุดใน `importBookAndRecompute`), `.header-stats-kpis` + `.stat-box` (ยัง live ใน index), `verifyAll()`/`loadCountsToTable()` (ลบเฉพาะ binding ของปุ่มที่ไม่มี ไม่ใช่ตัวฟังก์ชัน) · ตรวจ CSS หลังลบ: วงเล็บปีกกาสมดุล 275/275 ไม่มี rule ว่างค้าง · smoke: index KPI 3 การ์ดขึ้นครบ (0/0 · 40 · 96%), reconcile export เหลือ 79 key, `uiConfirm.parseMessage` = undefined แต่ `show`/`twoStep` ยังทำงาน, ไม่มี 404 |
| 2026-08-11 | **แก้ regression + เก็บงานตาม review** (ถอดคู่มือ + dead code ชุด 6-9) | 🔴 `Html/audit_check.html` ลบ `if (btn) btn.disabled = false;` 2 จุดใน `finally` ที่กลายเป็นตัวแปรกำพร้า หลังลบ `const btn` ไปพร้อมปุ่มที่ไม่มีใน markup · `Js/reconcile-shared.js` ลบค่าคงที่กำพร้า `ADJ_APPLY_PARALLEL`/`ACCEPT_MATCH_CHUNK` · แก้เอกสาร: ISSUES L1/L2 กลับเป็นเปิด (ปิดได้แค่บางส่วน), ARCHITECTURE (แถวตาราง localStorage ผิดรูป + จำนวนบรรทัด CSS), FIX_TRACKING (export 84→78 ที่จริงคือ 85→79 + เพิ่ม audit_check เข้า Smoke Checklist), `pages/audit_check.md`, agent 2 ไฟล์, CLAUDE.md · ลบโฟลเดอร์ `assets/` ที่ว่างแล้ว | 267 PASS / 0 FAIL / 2 KNOWN-OPEN (เท่าเดิม) | ⚠️ **ใช่ — code-reviewer จับ regression ได้ 1 ข้อ** | **บั๊กที่เกิดจริง**: ลบ `const btn = document.getElementById('btnVerifyAll')` ออก (ปุ่มไม่มีใน markup) แต่ลืมบรรทัดที่ใช้มันใน `finally` ⇒ `ReferenceError: btn is not defined` ทุกครั้งที่เรียก `verifyAll()` / `loadCountsToTable()` ⇒ **pipeline ตรวจสอบตายทั้งหน้า** (เปิดหน้า/เปลี่ยนคลัง = toast แดง, ปุ่ม "ยืนยันว่าปกติ" เขียน DB สำเร็จแต่รายงานว่าไม่สำเร็จ, โหมดเทียบตำแหน่งพัง) · **เทส 267 ข้อผ่านหมด** เพราะไม่มีเทสไหนรัน inline script ของ HTML 5,000 บรรทัด และโปรเจกต์ไม่มี linter · **สาเหตุเชิงกระบวนการ**: Impact Map บอกให้ smoke audit_check เมื่อแตะไฟล์นี้ แต่รอบนั้น smoke แค่ index กับ reconcile → เพิ่มแถว audit_check เข้า Smoke Checklist พร้อมเกณฑ์ชัดเจน ("แถบสถิติต้องไม่เป็น 0 ทั้งแถว") · **ลองเขียนเทสยามอัตโนมัติแล้วถอย**: ทำ scope analyzer ด้วย regex เพื่อจับ "ตัวแปรถูกใช้แต่ไม่มีตัวประกาศ" แต่รุ่นแรกลบ string ทีละชนิดทำให้ `'` ใน `"..."` จับคู่ผิด กลืนไฟล์ 120K อักษร (เห็นฟังก์ชัน 22 จาก 137) แล้ว **รายงาน PASS ทั้งที่ไม่ได้ตรวจอะไรเลย** · แก้เป็น tokenizer เดินครั้งเดียว + self-check จำนวนฟังก์ชันแล้ว ยังเจอ false positive ต่อเนื่อง (destructured params, arrow ซ้อน, regex literal) ⇒ **ตัดสินใจถอยทิ้ง** เพราะเทสที่รายงานปัญหาปลอมอันตรายกว่าไม่มีเทส (สอนให้คนเมินผลแดง) — การเขียน JS parser ด้วย regex ไม่คุ้ม |
| 2026-08-11 | **M6 + M9 — เสร็จ** (ชุด "บันทึกผลนับต้องไม่ซ้ำ ไม่หาย") | `Html/import_counts.html` (M6: `failedRows` + `pendingValidRows` พาคีย์เดิมไปด้วย · payload ใช้ `r.client_request_id || genClientRequestId()` · `batchImportId` สืบทอดจากแถวที่ค้าง) · `Js/script.js` (M9: `insertGroupRowsOneByOne()` ใหม่ + เปลี่ยน all-or-nothing เป็น fallback รายแถว + `groupItems` เหลือเฉพาะแถวที่พัง + toast สรุปผลจริง) · เทสใหม่ `unit/import-retry-idempotent` × 9 · cache-buster `script.js?v=20260811d` (index หน้าเดียว) | ก่อน: 267/0/2 → หลัง: **276 PASS / 0 FAIL / 2 KNOWN-OPEN** | ไม่มี | **M6 อันตรายสุดในลิสต์ M** เพราะเป็นข้อเดียวที่ทำให้เกิดแถวซ้ำ**จริง**ใน `inventory_counts` — เน็ตสะดุดตอนนำเข้า แถวเข้าไปแล้วแต่ client ไม่ได้ response → กดซ้ำ → เดิม mint `client_request_id` ใหม่ ⇒ unique index มองเป็นคนละแถว ⇒ แทรกซ้ำ · ขัดหลักการ "นับมายังไงเก็บอย่างนั้น" โดยตรง · **M9 กระทบโหมดที่ admin ใช้บ่อยสุด** (แบบกลุ่ม 25 รายการ) — 1 แถวพัง = อีก 24 แถวถูก rollback ทั้งที่ถูกต้อง · `import_counts` มี fallback แบบนี้อยู่แล้วแต่ไม่ได้แชร์โค้ด · **เทสเป็นแบบสแกน source** เพราะทั้ง 2 จุดเป็น inline script / IIFE ที่ยกมารันแยกไม่ได้ — จึงมีเทสข้อแรกยืนยันว่าอ่านบล็อกเจอจริงก่อนเสมอ (บทเรียนจากยามที่เคย "ผ่านทั้งที่ไม่ได้ตรวจ" เมื่อเช้า) และพิสูจน์ด้วย mutation ทั้ง 2 ข้อ · ⚠️ **ไม่ได้ smoke เคส partial failure จริงในเบราว์เซอร์** — ต้องจงใจสร้างแถวที่ทำให้ DB ปฏิเสธ ซึ่งจะเขียนข้อมูลทดสอบลงฐานจริงของ admin · ยืนยันแค่ว่าหน้านับโหลดปกติ + staging กลุ่มทำงาน (2/25) + ไม่มี error |
| 2026-08-11 | **เก็บงานตาม review M6/M9** | `Html/import_counts.html` (+ `savePendingKeys`/`loadPendingKeyMap`/`restorePendingKeys` เก็บคีย์ค้างลง localStorage · `logImportBatch` รับ `previewRows` แทนการอ่าน `pendingValidRows` ที่ถูกแทนที่ไปแล้ว · `canExport` ยอมให้ export เมื่อ `record_id` เป็น UUID แม้ ok=0) · `Js/script.js` (จับคู่ `insertedPairs` ด้วยดัชนีแทน SKU queue · `groupItems` reverse กลับ · `logAudit` ยังเขียนแม้ `.select()` คืนว่าง · toast duplicate บอกให้รีเฟรช) · **เขียนเทสใหม่ทั้งไฟล์** `unit/import-retry-idempotent` 11 ข้อ (5 ข้อ `[behaviour]` รันโค้ดจริงผ่าน mock DB) · `docs/pages/index.md` + `import_counts.md` + ISSUES (M6 ขอบเขต + M31 + M32 ใหม่) | 276 → **278 PASS / 0 FAIL / 2 KNOWN-OPEN** | ไม่มี | 🔴 **review จับได้ว่าเทสรุ่นแรกไม่จับของจริงทั้ง M6 และ M9** — พิสูจน์ด้วย mutation: เปลี่ยน `buildCountPayload` ให้ mint คีย์ใหม่ (= บั๊ก M6 ต้นฉบับเป๊ะ ๆ) ผ่าน 276/276 · แทรก `if (error) { throw error; }` คนละบรรทัด (= บั๊ก M9 ต้นฉบับ) ผ่าน 276/276 · สาเหตุ: เทสจ้อง "หน้าตาโค้ดตรงจุดที่ผมแก้" ไม่ได้จ้อง **ผู้บริโภคปลายทาง** (`buildCountPayload`) และ regex `if \(error\) throw error;` แพ้แค่การขึ้นบรรทัดใหม่ · **นี่เป็นครั้งที่ 3 ในวันเดียวที่ผมเขียนยามที่ไม่ยาม** → เปลี่ยนแนวเป็นยกฟังก์ชันจากซอร์สจริงมารันใน `vm` + mock DB (แบบเดียวกับ `audit-loc-compare`) แทนการอ่านซอร์ส · ข้อที่ยังต้องอ่านซอร์สเหลือเฉพาะเส้นทางที่พันกับ DOM และเปลี่ยนเกณฑ์เป็น "ห้ามมี throw คั่นระหว่าง bulk กับ fallback" ซึ่งไม่แพ้การจัดรูปแบบ · 🔴 **และ M6 ปิดไม่สนิท** — คีย์อยู่ใน memory ล้วน ⇒ refresh / อัปไฟล์เดิมทับ = บั๊กเดิมกลับมา 100% ซึ่งเป็นสิ่งที่คนทำเป็นอันดับแรกเมื่อเน็ตหลุด (เคสเดียวที่ M6 อันตรายจริง) → เพิ่ม localStorage + กู้คีย์ตอน parse ไฟล์ · พิสูจน์ในเบราว์เซอร์: ใส่คีย์ค้างจำลอง → `restorePendingKeys` คืน `orig-key-1`/`batch-X` ให้แถวที่ตรง และไม่แตะแถวใหม่ |
| 2026-08-11 | **ชุด "รอบต้องถูกต้อง" — M19 + M24 + M1 เสร็จ** | `Js/reconcile-shared.js` (M19: tiebreak `localeCompare` + dedupe ใน `encodeCycleWarehouses` · M24: `isCycleClosed()` + guard ใน `isCycleRelevantNow` · M1: `fetchCountMonths()` + `fetchCountDaysInMonth()` ใหม่) · `Js/script.js` (M24: `filterCyclesCurrentMonth` กรองรอบปิด) · `Html/cycle_config.html` (M1: เลิกใช้ `.limit(10000)`/`.limit(5000)` ทั้ง 3 จุด) · เทสใหม่ `dryrun/cycle-scope` × 12 + ย้าย M19 จาก knownIssue → ถาวร 4 ข้อ · cache-buster `20260811f` | ก่อน: 278/0/2 → หลัง: **296 PASS / 0 FAIL / 1 KNOWN-OPEN** (เหลือแค่ M2) | ไม่มี | **M1 รากของบั๊กคือ `.limit()` ไม่ใช่ตัวเลขน้อยไป** — Supabase hosted จำกัด max-rows ที่ 1,000 และ**ตัดเงียบ ๆ** `.limit(10000)` จึงได้แค่ 1,000 ⇒ วัน/เดือนที่มีการนับจริงหายจาก dropdown โดยไม่มีอะไรเตือน ⇒ ผูกผลนับเข้ารอบไม่ครบ · เดือนใช้ RPC `get_inventory_count_months` ที่มีอยู่แล้วตั้งแต่ 013 (audit_check ใช้อยู่ แต่ cycle_config ไม่เคยเรียก) · **M19 จุดที่ต้องระวัง**: แก้ได้แค่ tiebreak ของคลัง**นอก**รายการมาตรฐาน ห้ามเปลี่ยนไปเรียงตามชื่อทั้งหมด เพราะรอบเก่าใน DB เก็บสตริงตามลำดับ registry อยู่ — ถ้าเปลี่ยนจะกลายเป็นสร้างรอบซ้ำเสียเอง (มีเทสบังคับ) · **M24 ต้องแก้ 2 ฝั่งพร้อมกัน** ตามที่ ISSUES เขียนแนวทางไว้ · mutation ทั้ง 6 แบบทำให้เทสแดงตรงจุด · smoke กับข้อมูลจริง: `fetchCountMonths` ยิง RPC จริง ได้ 3 เดือน · `fetchCountDaysInMonth` ได้วัน 06–11 ของเดือนปัจจุบันครบ · ไม่มี 404 |
| 2026-08-11 | **แก้ตาม code-review ชุด 2** | `Js/reconcile-shared.js` (`fetchCountMonths` ผ่าน `parseCycleWarehouses` แล้วยิง RPC ทีละคลัง · เดินหน้าตามจำนวนแถวที่ได้จริง) · `Html/count_search.html` (`.limit(8000)` → helper) · `Html/import_counts.html` + `Js/script.js` (ข้อความ "รอบปิดแล้ว") · `Html/cycle_config.html` (ลบ `createdAtToBangkokYmd` กำพร้า) · `tests/helpers/lift.mjs` ใหม่ · เทส +2 | **296 PASS / 0 FAIL / 1 KNOWN-OPEN** | ไม่มี | 🔴 **regression ที่ review จับได้ อันตรายกว่าบั๊กเดิม**: `fetchCountMonths` ส่งค่า encode ('คลังทั้งหมด' / 'A|B') เข้า RPC ดิบ ๆ แต่ RPC เทียบ `warehouse = p_warehouse` ตรง ๆ ⇒ โหมดทุกคลังได้ **0 เดือน** ทั้งที่มีข้อมูลเป็นหมื่นแถว (บั๊กเดิมอย่างน้อยยังได้ 1,000 แถวแรก) · **เทสไม่จับเพราะยิงด้วยชื่อคลังเดี่ยวอย่างเดียว และ smoke ก็ทดสอบเคสเดียวกัน** ⇒ บทเรียน: เทส helper ต้องครอบ *ทุกรูปแบบของค่า* ที่ call site ส่งเข้ามาจริง ไม่ใช่รูปแบบที่คิดถึงก่อน · **เทสยามที่ไม่ยาม (ครั้งที่ 4)**: `[M24] dropdown` เป็น source-scan review ลบ `.filter()` ที่ใช้จริงโดยเก็บบรรทัดประกาศไว้ แล้วเทสเขียว 294/294 ⇒ เปลี่ยนเป็น behaviour test ยกฟังก์ชันจริงมารันผ่าน `tests/helpers/lift.mjs` (helper ใหม่ ใช้ร่วมกับ import-retry) · **เทสที่มองแค่ไฟล์ที่ตัวเองแก้** ก็ไม่ยามเหมือนกัน: `.limit()` scan ดูแค่ `cycle_config` เลยไม่เห็น `count_search` ที่บั๊กเดียวกันเป๊ะ ⇒ สแกนทั้ง repo แล้ว · mutation ทั้ง 4 แบบทำให้เทสแดงตรงจุด · smoke ยืนยัน: ทุกคลัง/หลายคลัง/คลังเดียว ได้ 3 เดือนเท่ากัน (ก่อนแก้ ทุกคลัง = `[]`) |
| 2026-08-11 | **ชุด "ตัวเลขหน้า Match" — M2 + M18 + M3 เสร็จ (รวมรัน migration จริง)** | `docs/sql/020_match_status_count_only_in_book.sql` ใหม่ (M2 — **รันกับ Supabase จริงแล้ว** + refresh ครบ 6 รอบ) · `Html/reconcile.html` (M18 `formatRowVariancePct` คำนวณใหม่ · M3 `computeDisplayVariance` คืนทิศทาง + `buildMatchExportRows` แยกออกมา + `buildAdjustedExportRows` กลับเครื่องหมาย) · เทสใหม่ `dryrun/match-status` × 13 + ย้าย M2 จาก knownIssue → ถาวร | ก่อน: 296/0/1 → หลัง: **309 PASS / 0 FAIL / 0 KNOWN-OPEN** (ครั้งแรกที่ไม่มี KNOWN-OPEN เหลือเลย) | ไม่มี | **ผลกับ DB จริง**: 77 แถวเปลี่ยน `count_only` → `over` · ไม่มีแถวหาย/เพิ่ม · `variance_qty`/`variance_pct` ไม่ขยับเลยแม้แต่แถวเดียว (สำรองไว้เทียบก่อน-หลังแล้วลบทิ้ง) · การ์ด "ความต่าง (ชิ้น)" ของ dashboard เพิ่ม 411 ชิ้น (ตึกกันตนา 2026-08) และ 14 (2026-06) — ไม่ใช่ข้อมูลใหม่ แค่ย้ายมาจากช่องที่จัดประเภทผิด · ⛔ **`CREATE OR REPLACE FUNCTION` เขียนทับ attribute ทั้งชุด** (SECURITY/search_path/VOLATILE/STRICT/COST) — 018 ตั้ง `SECURITY DEFINER` ด้วย `ALTER` จึงหายถ้าไม่ประกาศซ้ำ แล้วจะ 401 ทันที (owner/GRANT/COMMENT ไม่หาย เพราะ oid เดิม) · 🔴 **เทสสแกน SQL รุ่นแรกปล่อย mutant รอด 3 แบบ** (สลับ over↔count_only, ลบสาขา book_only ทั้งบรรทัด, ลบ `UPDATE book_stock_lines` ท้ายฟังก์ชัน) — ตัวที่ 2 รอดเพราะ `indexOf` คืน `-1` แล้ว `slice(start, -1)` กลายเป็นเกือบทั้งไฟล์ ⇒ เจอทุกคำที่หา · แก้เป็น **แปล `CASE` จากไฟล์ SQL เป็นฟังก์ชัน JS แล้วรันเทียบ `computeMatchStatus` ทุกเคส** + diff body กับ 013 ว่าต่างเฉพาะ 3 hunk ที่ตั้งใจ · ตัวแปลจับบั๊กในตัวเทสเองได้ทันที (ลืม strip `m.` ออกจากชื่อคอลัมน์) · mutation ทั้ง 4 แบบแดงตรงจุด · **ทิศทาง variance เปลี่ยนกระทบ `stock_adjustments.variance_before`** ซึ่ง write-only แต่แถวเก่า/ใหม่คนละ convention — บันทึกใน DATABASE.md แล้ว · smoke: ขาดแสดง -1/-10 (เดิม +1/+10) · เกิน +13 · BB086 Book 0 นับ 1 = "เกิน" · BG247 ไม่มีใน Book = "ไม่พบใน Excel" · แถวรวม Export สุทธิ +1,396 ตรงกับ 38,687−37,291 เป๊ะ (ขาดรวม 3,484 · เกินรวม 4,880) |
| 2026-08-11 | **ชุด "หน้า audit_check" — M12 + M13 + M34 (+M11 ที่ปิดไปแล้วพร้อม M1)** | `Html/audit_check.html` (M12 `fetchDestinationRowsFromDb` + ดัชนี `refBySkuLocQty` + `validateDestUpdateBatch` เป็น async · M13 normalize `destLoc` · M34 ใช้ helper ร่วม) · `Js/count-scan-shared.js` **ใหม่** · `Js/reconcile-shared.js` (delegate) · `<script>` + cache-buster `20260811g` ทุกหน้า · เทสใหม่ `dryrun/audit-dest-guard` × 12 | ก่อน: 309/0/0 → หลัง: **321 PASS / 0 FAIL / 0 KNOWN-OPEN** | ไม่มี | 🔴 **M12 เป็นช่องโหว่จริงของ invariant ข้อ 3** — guard เดิมดูแต่ `refBySkuLoc` ที่โหลดมาเฉพาะคลัง+เดือนที่เลือก ⇒ ถ้าปลายทางไปชนแถวนอก scope จะปล่อยผ่านเงียบ ๆ แล้วได้แถวซ้ำจริงในรอบเดียวกัน = Match บวกยอดซ้ำ · ยืนยันในเบราว์เซอร์ว่า query ใหม่เห็นแถวข้ามคลัง/เดือนจริง (`BY002@C4-01` มี 2 แถว) ใช้ 215 ms ต่อ 3 SKU · ⚠️ **กรอง `location` ฝั่ง DB ไม่ได้** — ฐานจริงมีตำแหน่งตัวพิมพ์เล็ก 142 แถว (270 ตำแหน่ง collapse เหลือ 269 หลัง normalize) ต้องกรองฝั่ง client · **อ่านปลายทางไม่สำเร็จ = บล็อกทั้งชุด** เพราะ "ตรวจไม่ได้" ไม่เท่ากับ "ไม่ชน" · **M13 แก้ได้แค่ครึ่งเดียวโดยธรรมชาติ** — ตัวพิมพ์เดิมของตำแหน่งหายตั้งแต่รอบแรกเพราะมันกลายเป็น SKU ที่ต้อง UPPERCASE (invariant ข้อ 2) สิ่งที่รับประกันได้คือตั้งแต่รอบ 2 เป็นต้นไปต้องนิ่ง · **M34 ทำให้ `<script>` order กลายเป็นเรื่องคอขาดบาดตาย** — เพิ่มเทสบังคับว่า count-scan-shared ต้องมาก่อน reconcile-shared ทุกหน้า · 🔴 **เทสอ่าน field ผิดที่แล้วผ่านแบบว่างเปล่า** — mock เก็บ `eq/in/gte` ใน `filters` แต่ `order/range` ใน `modifiers` ข้อ "ห้ามกรอง scope" จึงเช็คกับ array ว่างและผ่านฟรี ๆ · เจอตอน mutation ไม่ใช่ตอนเขียน · mutation ทั้ง 8 แบบแดงตรงจุด · smoke: audit_check โหลด 1,152 แถว ถูกต้อง 1,152 / ผิด 0 · dropdown เดือนมาจาก helper ร่วม · ไม่มี console error |
| 2026-08-11 | **แก้ตาม code-review ชุด 4** | `Html/audit_check.html` (`fetchMovingRowsMeta` ใหม่ · `scopeNoCycleRowsToSameMonth` ใหม่ · `.in('counted_qty')` · overlay+กันกดซ้ำ 3 call site · normalize location ครบ 3 ทางเขียน · ลบ `getThailandYearMonth` ที่ตาย) · `Js/reconcile-shared.js` (ลบ `bangkokYmdOf` ที่ตายหลังย้าย M34) · เทส +9 | **331 PASS / 0 FAIL / 0 KNOWN-OPEN** | ไม่มี | 🔴 **M12 รอบแรกปิดรูไม่หมด** — เปิดตาให้ฝั่ง "ปลายทาง" เห็นทั้งฐาน แต่ฝั่ง "แถวที่กำลังย้าย" ยังอ่านรอบจาก memory ⇒ ไม่สมมาตร · แถวที่ `location` ว่างไม่เข้า `refBySkuLoc` เลย (loadReferenceData ข้าม) และนั่นคือแถวที่คนเปิดโหมดแก้ตำแหน่งมาเติมพอดี ⇒ **เส้นทางที่ใช้บ่อยที่สุดคือเส้นที่ guard อ่อนที่สุด** · แก้ด้วยการอ่าน `cycle_id` ของแถวที่ย้ายจาก DB ตรง ๆ · 🔴 **`cycleKey(null)` เป็นค่าเดียวกันทั้งฐาน** — พอ M12 เปิดให้เห็นทั้งฐาน แถวที่ยังไม่ผูกรอบของปีที่แล้วจะบล็อกการแก้ตำแหน่งของเดือนนี้ ด้วยข้อความ "รอบนับเดียวกัน" ที่ไม่จริง และกดผ่านไม่ได้เลย · เกิดง่ายเพราะ FK เป็น `ON DELETE SET NULL` (ลบรอบ 1 ครั้ง = แถวทั้งรอบกลายเป็นไม่มีรอบ) · จำกัดด้วยเดือนไทยแทน · ⚠️ **`inventory_counts` ไม่มี index บน `sku_id`** — แคบด้วย `.in('counted_qty')` ช่วยได้ทันที (คีย์ใช้ qty อยู่แล้วจึงไม่พลาดแถว) เปิด M36 ไว้ทำ migration · 🔴 **`\\s` ในสตริงที่สร้าง RegExp หายไปเงียบ ๆ** — `new RegExp(\`...\\s...\`)` กลายเป็น `s` เฉย ๆ แล้วเทสผ่านแบบว่างเปล่า ⇒ เลิกสร้าง RegExp จากสตริง ใช้ `lastIndexOf` แทน · mutation 9 แบบของ review + 9 แบบของผม: แดงตรงจุดทุกอันยกเว้น 1 ที่พิสูจน์แล้วว่าเป็น no-op จริง (`refEntryKey` norm ให้อยู่แล้ว) · smoke: audit_check 1,152/1,152 ถูกต้อง · โหมดแก้ตำแหน่ง/สลับ เปิดปิดได้ปกติ · ไม่มี console error |
| 2026-08-11 | **ชุด "หน้านับสต็อก" — M8 + M10 + M32 เสร็จ** | `Js/script.js` (M8 `getEditDestinationCollision` เป็น async ถาม DB + ใช้เกณฑ์ร่วมกับ audit_check · M10 `updateStats` แยก `bookPending` · M32 circuit breaker) · `Html/import_counts.html` (M32) · `index.html` (โหลด `audit-dedupe.js` + ค่าเริ่มต้น KPI + cache-buster `20260811h`) · เทสใหม่ `dryrun/count-page-guard` × 17 | ก่อน: 331/0/0 → หลัง: **348 PASS / 0 FAIL / 0 KNOWN-OPEN** | ไม่มี | 🔴 **M8 ผิด 2 ชั้นพร้อมกัน** — บล็อกงานที่ถูกต้อง (นับแยกถุง ขัด invariant ข้อ 3 + migration 011) **และ** ปล่อยงานที่ผิดจริงผ่าน (ดูแต่ cache ของ scope ปัจจุบัน) · ยืนยันกับข้อมูลจริง `BNP20 @ B2-01` = 70 + 200: เดิมแก้แถวไหนก็โดนบล็อกทั้งที่ไม่ซ้ำเลย · **บทเรียนซ้ำจาก M12**: guard ที่ตัดสินจาก cache ของ scope ปัจจุบันจะให้คำตอบต่างกันตามตัวกรองที่ผู้ใช้เลือก = ไม่ deterministic · **เกณฑ์ "ซ้ำจริง" ต้องอยู่ที่เดียว** — index โหลด `audit-dedupe.js` ตัวเดียวกับ audit_check แล้ว ไม่งั้น 2 หน้าตัดสินคนละแบบและ drift ตามกาลเวลา (บทเรียนเดียวกับ M34) · **M10 ต้องแก้ markup ด้วย** ไม่ใช่แค่ JS — ก่อน JS รันผู้ใช้เห็น `0%` อยู่แล้ว · **M32 แถวที่ถูกข้ามต้องพาคีย์เดิมไปด้วย** ไม่งั้นกด "นำเข้าแถวที่เหลือ" แล้วแทรกซ้ำ (บั๊ก M6 กลับมาทางอ้อม) · mutation 15 แบบแดงตรงจุดทุกอัน |
| 2026-08-11 | **แก้ตาม code-review ชุด 5** | `Js/script.js` (paging + เพดานรอบใน guard · `edBusy`/`edGeneration`/ยาม stale · คลังว่างบล็อก · norm ชื่อคลัง · `refreshUncounted` รู้จัก bookPending · ไม่มี Book = `—` ทั้ง 2 กล่อง · toast บอก abort) · `Html/import_counts.html` (chunk loop หยุดจริงเมื่อ abort) · `tests/helpers/lift.mjs` (รองรับ `window.f = function`) · เทส +10 | **358 PASS / 0 FAIL / 0 KNOWN-OPEN** | ไม่มี | 🔴 **บทเรียน M1 ซ้ำรอบที่ 3** — guard ใหม่ยิง query โดยไม่แบ่งหน้า · PostgREST ตัดที่ 1,000 เงียบ ๆ และเพราะ `.order('id')` ขึ้น แถวที่ถูกตัดคือ**แถวใหม่สุด** = แถวรอบปัจจุบันที่ guard มีไว้จับพอดี · 🔴 **การเปลี่ยนฟังก์ชันให้เป็น async สร้าง race ใหม่** — `handleEdConfirm` เดิมไม่มี await นำหน้าการเขียนเลย พอมี 4 round-trip แล้วไม่มียาม ⇒ กดยกเลิกแล้วเปิดแถวอื่นระหว่างรอ = เขียน **audit log ของการแก้ไขที่ไม่เคยเกิด** (ขัด invariant ข้อ 1) · 🔴 **เทส regex ล้วนยามไม่ได้ (ครั้งที่ 5)** — review ใส่ `const bookPending = false;` แล้วเทส M10 ทั้ง 3 ข้อยังเขียว ⇒ ยก `updateStats`/`refreshUncounted` มารันจริงกับ DOM ปลอม · ต้องขยาย `lift.mjs` ให้ยก `window.f = function` ได้ · ⚠️ **คืนค่าสถานะมาแล้วต้องมีคนอ่าน** — `aborted` ของ import เป็น write-only ทั้ง 2 หน้า ⇒ circuit breaker แทบไม่ได้ผลตามที่ตั้งใจ · ⚠️ **mutation เปิดเผยความเปราะที่เทสจับไม่ได้** — ถอด `.range()` แล้วลูปวนไม่จบจนเทสค้าง (ไม่ใช่ FAIL) ⇒ ใส่เพดานรอบ · mutation รอบ 2: 12 แบบแดงตรงจุดทุกอัน (รวม 2 ตัวที่ review พิสูจน์ว่ารอดในรุ่นแรก) · smoke: หน้านับ KPI ถูกต้อง กดกล่อง "ยังไม่ได้นับ" แล้ว badge ไม่เพี้ยน · import_counts โหลดปกติ · ไม่มี console error |
| 2026-08-11 | 🔴 **แก้บั๊กที่หลุดถึงผู้ใช้ — บันทึกกลุ่มไม่ได้เลย** | `Js/script.js` (`one.aborted` อยู่นอก scope → `groupAborted` · ลำดับข้อความ toast) · `tests/unit/scope-guard.test.mjs` ใหม่ (รัน `submitGroup` + `handleEdConfirm` จริง) · `tests/helpers/lift.mjs` (เช็คขอบคำ) · cache-buster `20260811i` | **364 PASS / 0 FAIL / 0 KNOWN-OPEN** | 🔴 **มี — หลุดถึงผู้ใช้** | `const one` ประกาศในบล็อก `else` แต่ผมเอา `one.aborted` ไปใช้นอกบล็อก ⇒ `ReferenceError` ทุกครั้งที่กดส่งกลุ่ม · **เทส 348 ข้อผ่านหมด** เพราะยกเฉพาะ `insertGroupRowsOneByOne` (ลูก) มารัน ส่วน `submitGroup` (แม่) มีแต่เทสอ่านซอร์ส · `new Function(src)` ที่ผมใช้เช็คตรวจแค่ syntax ไม่ใช่ scope · **วัดขอบเขตความเสี่ยง**: ฟังก์ชันที่แก้ในชุด 1–5 มี 88 จุด **มีเทสรันจริงแค่ 29** · พบบั๊กใน `lift.mjs` เองด้วย — ยกฟังก์ชันที่ชื่อเป็น prefix ของอีกตัวได้ผิดตัวเงียบ ๆ · พิสูจน์แล้วว่าเทสใหม่จับบั๊กนี้ได้ (เอากลับมาแล้วแดง 3 ข้อ) |
| 2026-08-11 | **เพิ่มเทส "รันจริง" ให้ปุ่มที่เขียนข้อมูลครบทุกตัว** | `tests/unit/write-path-runtime.test.mjs` ใหม่ — รัน `applySwapSkuLocSelected` · `saveLocationChanges` · `applyLocCompareUpdates` · `runImport` จริงผ่าน DOM ปลอม + client ที่บันทึกทุก write | **370 PASS / 0 FAIL / 0 KNOWN-OPEN** | ไม่มี | ต่อจากบั๊ก `submitGroup` ที่หลุดถึงผู้ใช้ · เป้าหมายไม่ใช่เทส business logic (มีไฟล์อื่นคุมอยู่) แต่คือ **พิสูจน์ว่ากดปุ่มแล้วโค้ดวิ่งจนจบ** และเขียน DB ตามที่ตั้งใจ · ระหว่างเขียนต้องเติม stub 12 รอบ — แต่ละรอบคือ dependency จริงที่ฟังก์ชันนั้นพึ่งอยู่ ซึ่งไม่มีใครเคยรู้เพราะไม่เคยมีใครรันมัน · ⚠️ `deepStrictEqual` ข้าม realm ของ `vm` ไม่ได้ (prototype คนละตัว) เจอซ้ำครั้งที่ 2 ⇒ เทียบผ่าน `JSON.stringify` · mutation 6 แบบ (จำลองตัวแปรผิด scope ทุกปุ่ม + เขียนค่าดิบ + ลืม audit log): แดง 5 · อีก 1 พิสูจน์แล้วว่าเป็น no-op จริง (flush audit log กลางลูปหาย แต่ตัวปิดท้ายยังบังคับเขียน) · **ช่องว่างลดจาก 29/88 เป็น 36/90** — ปุ่มที่เขียน `inventory_counts` ครอบครบทุกตัวแล้ว |
| 2026-08-11 | **ฟีเจอร์ใหม่: ตรวจ "ซ้ำข้ามตำแหน่ง" ในหน้า audit_check** | `Js/audit-dedupe.js` (`findCrossLocationDuplicates`) · `Html/audit_check.html` (สถานะ `crossloc` + การ์ดสถิติ + ตัวกรอง + โหมดเรียงจับคู่ + เส้นคั่น + ต่อเข้าปุ่ม "ยืนยันว่าปกติ") · เทสใหม่ `unit/cross-location-dup` × 17 · cache-buster `20260811j` | **389 PASS / 0 FAIL / 0 KNOWN-OPEN** | ไม่มี | admin เจอด้วยตาก่อน: SKU เดียวกัน จำนวนเท่ากันเป๊ะ ต่างกันแค่ตำแหน่ง · ตรวจกับฐานจริงแล้วเป็นการนับชั้นเดิมซ้ำใต้ป้ายคนละชื่อ (K3-01/L4-01, K3-02/L4-02, K3-03/L4-03 …) · `PK089` = 256 ทั้งสองที่ Book 258 ⇒ Match เกิน 254 · `PC697` = 329+329 Book 331 · ทั้งฐาน 105 กลุ่ม / 211 แถว — รอบ 2026-08 ตอนตรวจล่าสุด 12 คู่ / 24 แถว / 958 ชิ้นที่น่าจะเกิน (ตรงกับที่หน้าเว็บแสดงเป๊ะ) · **Match รวมยอดด้วย SUM ต่อ SKU ต่อรอบโดยไม่สนตำแหน่ง** จึงบวกซ้ำเต็มจำนวน — ต่างจาก "ทับซ้อน" ที่เป็นตำแหน่งเดียวกันจำนวนต่างกัน · ⚠️ เตือนอย่างเดียวตามนโยบายข้อ 3 (มีเทสบังคับว่าห้ามมีคำแนะนำให้ลบ) · ปิดเสียงผ่านปุ่ม "ยืนยันว่าปกติ" เดิม · admin ขอเพิ่มระหว่างทาง: คู่ที่ซ้ำต้องอยู่ติดกัน (เดิมอยู่แถว 941 กับ 1005) ⇒ โหมดเรียง `crossloc` + สลับให้อัตโนมัติเมื่อกดการ์ด + เส้นคั่นสีม่วง |
| 2026-08-11 | **ฟีเจอร์ใหม่: ปุ่ม "Export ยอดจริง" ในหน้า Match + กู้คืนการยืนยันของรอบ 2026-08** | `Html/reconcile.html` (`buildFinalQtyExportRows` + ปุ่ม `btnExportFinal` + ชีต `ยอดจริง`) · เทสใหม่ `dryrun/final-qty-export` × 8 · **ปฏิบัติการข้อมูล (มติ admin)**: ลบ `reconciliation_match_acceptances` 168 แถวของรอบตึกกันตนา 2026-08 + `stock_adjustments` 2 แถว (BB096 +1, BP022 +2 — เขียน `RECONCILE_ADJ_CLEAR` ก่อนลบ) + refresh 1,114 แถว | **397 PASS / 0 FAIL / 0 KNOWN-OPEN** | ไม่มี | admin กดยืนยันขาด-เกินไป 168 รายการบนข้อมูลที่ยังมีแถวซ้ำ พอลบแถวซ้ำใน audit_check แล้วขอเริ่มยืนยันใหม่ ⇒ กู้เฉพาะรอบนี้ (รอบเก่า 733 รายการไม่แตะ) ตรวจก่อน-หลังครบ: BB096 กลับเป็น Excel 14/นับ 15/เกิน +1 · ที่มาของฟีเจอร์: ปุ่ม 2 ตัวความหมายต่างกัน — "ยืนยันเป็นถูกต้อง" = ยอด Excel คือยอดจริง · "ยอมรับผลนับ" = ผลนับคือยอดจริง — แต่ export เดิมไม่บอก ⇒ admin สรุปยอดไม่ได้ว่าต้องเชื่อเลขไหน · **แถวที่ยังไม่ตัดสินจงใจเว้นช่องว่าง ไม่เดาแทนคน** (นโยบายข้อ 3) แถวรวมนับเฉพาะที่ยืนยันแล้ว + บอกจำนวนค้าง · smoke จริง 2 รอบ: 2026-06 ยืนยันครบ → รวม 36,825 ชิ้น ค้าง 0 · 2026-08 หลังกู้ → ยืนยันแล้ว 479 / ค้าง 635 / รวมเฉพาะที่ยืนยัน 10,950 |
| 2026-08-11 | **ฟีเจอร์ใหม่: โหมด "แก้ไขจำนวน" ในหน้า audit_check** | `Html/audit_check.html` (ปุ่ม+banner+CSS+`saveQtyChanges` โครงเดียวกับโหมดแก้ตำแหน่ง) · `Js/audit-log.js` (`EDIT_QTY` → bump `20260811k`) · `Js/script.js` (ป้ายประวัติ `AUDIT_EDIT_QTY` → bump `20260811k`) · เทสใหม่ `unit/edit-qty-mode` × 11 | **407 PASS / 0 FAIL / 0 KNOWN-OPEN** | เกือบมี — smoke จับได้ก่อน commit | โจทย์ admin: แก้จำนวนแล้วสถานะค้างที่ "ตำแหน่ง/จำนวนไม่ตรง" เพราะหน้านี้ไม่มีทางเขียนจำนวนลง DB เลย · ทางออก: โหมดแก้จำนวนที่เขียนจริง + guard M12 (จำนวนใหม่ชนแถวอื่นรอบเดียวกัน = บล็อก) + audit log ทุกแถว + ออกจากโหมดแล้ว re-verify ⇒ สถานะไม่ค้าง · 🔴 **ReferenceError รอบที่ 3 ของวัน** — เติม listener โดยอ้าง `qtyInput` ใน scope ที่ไม่มีตัวแปรนี้ ⇒ `createRow` พังทุกแถว ตารางว่างทั้งหน้า · เทส [ui] 9 ข้อจับไม่ได้เพราะไม่มีข้อไหนรัน `createRow` · **smoke ในเบราว์เซอร์จับได้ก่อน commit** (ตาม Smoke Checklist ที่บังคับหน้า audit_check) ⇒ เพิ่มเทสที่รัน `createRow` จริง · เทสยามเก่าจับของจริงได้ 2 จุดระหว่างทำ: `[M12] call site count` (บังคับให้ `saveQtyChanges` ต้อง await guard) และ `[audit-actions]` (บังคับเพิ่มป้าย `AUDIT_EDIT_QTY` ใน drawer ประวัติของ index ไม่งั้นประวัติขึ้นว่างเปล่า) · mutation 3 แบบแดงตรงจุด · smoke: เปิดโหมด → SKU/ตำแหน่งล็อก จำนวนแก้ได้ · dirty นับถูก · ปิดโหมด → สถานะกลับมา "ข้อมูลถูกต้อง" |
| 2026-08-11 | **ฟีเจอร์ใหม่ 2 ตัวใน reconcile: "ยอมรับเกิน (Apply เป็นชุด)" + "ประวัติ / คืนค่า"** | `Html/reconcile.html` (โมดัล 2 ตัว + `selectOverRowsForBulkAccept`/`buildBulkAcceptExportRows`/`runBulkAcceptOver` + `buildAdjustHistoryEntries`/`rollbackHistorySkus`) · เทสใหม่ `dryrun/bulk-accept-over` × 11 | **418 PASS / 0 FAIL / 0 KNOWN-OPEN** | ไม่มี | โจทย์ admin: (1) เลือกรายการเกินที่ต่าง +1 ถึง +N ดูรายการ/Export แล้วยอมรับผลนับทั้งชุดด้วยยืนยัน 2 ขั้น (2) ดูประวัติการปรับและกดคืนค่าได้เหมือน rollback ที่เคยทำให้ · ใช้ของที่มี+เทสแล้วทั้งหมด: `createStockAdjustmentsBatch` → `applyAllDraftsForCycle` (refresh ครั้งเดียว ไม่ใช่ทีละ SKU) และ `clearAdjustmentsAndMatchAcceptancesForSkus` (H6 เขียนประวัติก่อนลบ) · ⛔ **จุดอันตรายที่กันไว้: `apply_all_drafts_for_cycle` Apply ทุก draft ของรอบ** — มี draft ค้างอยู่ต้องบล็อกทั้งชุด ไม่งั้นพ่วง draft ที่ไม่เกี่ยวไปด้วย (มีเทสบังคับ) · เพดานผิดรูป (NaN/0/ลบ) = ลิสต์ว่าง ไม่ใช่เลือกทั้งหมด · mutation 4 แบบแดงตรงจุด (off-by-one เพดาน · ถอด guard draft · ลืม Apply · คืนค่าแล้วไม่ refresh) · **แก้ตาม feedback ทันที**: ปุ่มโผล่เฉพาะโหมด "เกิน" (ผูกกับ updateAutoAdjustButton + ซ่อนใน markup) · smoke จริง: เพดาน +10 → 177 SKU +484 ชิ้น · +3 → 136 SKU +198 ชิ้น (อัปเดตสด) · Export ตรงรายการ · ประวัติโชว์ 5 รายการที่ admin เพิ่งกดจริง · ติ๊กแล้วปุ่มคืนค่าตื่น · ไม่มี console error |
| 2026-08-11 | **ฝั่ง "ขาด" ของยอมรับเป็นชุด** | `Html/reconcile.html` — generalize เป็น `selectRowsForBulkAccept(lines, max, mode)` + ปุ่ม `btnBulkAcceptShort` (โผล่เฉพาะโหมดขาด) + โมดัล/Export/confirm ปรับตามโหมด · เทส +6 | **425 PASS / 0 FAIL / 0 KNOWN-OPEN** | ไม่มี | ขยายของเดิมแทนก็อป — เกณฑ์อยู่ฟังก์ชันเดียว แก้ทีเดียวได้ทั้งสองปุ่ม · ขาด: ต่าง −1 ถึง −เพดาน ยอดปรับติดลบ · mutation 3 แบบ: 2 แดงทันที · **ตัวที่ 3 รอด** (hardcode โหมดใน `refreshBulkAcceptList` = ผู้ใช้ตรวจรายการฝั่งเกินแต่ Apply ฝั่งขาด — อันตรายมาก) ⇒ เพิ่มเทสรัน `refreshBulkAcceptList` จริงว่ารายการที่แสดงต้องเป็นโหมดเดียวกับที่จะ Apply · smoke จริง: โหมดขาดเห็นเฉพาะปุ่มขาด (−1..−10 → 372 SKU −627 ชิ้น · −3 → 333 SKU −407 อัปเดตสด) · โหมดเกินเห็นเฉพาะปุ่มเกิน |
| 2026-08-13 | **ฟีเจอร์ใหม่: หน้า "ประวัติการปรับ / ยืนยัน" เต็มหน้า** (แยกจาก modal ของ reconcile) + ปิด finding จาก code review 6 ข้อ | ใหม่: `Html/adjust_history.html` · `Js/adjust-history-shared.js` · `docs/pages/adjust_history.md` · `tests/dryrun/adjust-history.test.mjs` · `tests/dryrun/adjust-fetch-paging.test.mjs` — แก้: `Js/reconcile-shared.js` (ย้าย `buildAdjustHistoryEntries` มาจาก inline + **แบ่งหน้า `fetchAdjustments` / `fetchMatchAcceptanceMap`**) · `Js/sidebar-shared.js` (เมนู + `ASSET_VER 20260813a`) · `Html/reconcile.html` (เรียก `RS.` + ปุ่มเปิดหน้าเต็ม) · cache-buster ทั้ง 12 หน้า · `tests/helpers/lift.mjs` (+`liftInto`) · `tests/unit/stable-paging.test.mjs` (+`NO_ID_TABLES`) | ก่อน: 425/0/0 → หลัง: **474 PASS / 0 FAIL / 0 KNOWN-OPEN** | ไม่มี | 🔴 **บั๊ก High ที่เทสและ smoke มองไม่เห็น — เจอโดย code review**: `fetchAdjustments` / `fetchMatchAcceptanceMap` **ไม่แบ่งหน้า** ⇒ PostgREST ตัดที่ 1,000 แถวเงียบ ๆ (บทเรียน M1 รอบที่ 4) กระทบทั้งหน้า Match และไฟล์ Export โดยจอยังบอก "แสดง N / N รายการ" ราวกับครบ · แก้แล้วพร้อม `.order('id')` ตาม invariant ข้อ 13 (`reconciliation_match_acceptances` ไม่มีคอลัมน์ `id` — PK เป็น (cycle_id, sku_id) จึงเรียง `sku_id`) · ⚠️ **`now instanceof Date` ใช้ข้าม realm ไม่ได้** — Date จาก vm/iframe ไม่ผ่าน instanceof แล้วเงียบ ๆ ตกไปใช้เวลาปัจจุบัน (cross-realm ครั้งที่ 3 ต่อจาก `deepStrictEqual`) · ⚠️ **หน้าที่มี filter + ปุ่มลบ/คืนค่า = ความเสี่ยงใหม่**: selectAll ต้องเลือกเฉพาะแถวที่เห็นหลังกรอง และเปลี่ยน filter/sort/รอบ ต้องล้าง selection · ⚠️ **error path ต้องล้าง `viewEntries` ด้วย ไม่ใช่แค่ `allEntries`** ไม่งั้นปุ่ม Export ค้างเปิดแล้วได้ไฟล์ของรอบก่อนหน้าติดป้ายรอบผิด · 🔴 **`Html/reconcile.html` ถูกแก้โดยกลไกนอกเหนือการแก้ของเรา ระหว่างเปิดค้างใน Browser pane** (rename `.rc-field` → `.rc-filter-field` ฯลฯ) และหลุดเข้า commit `4905e42` เพราะ `git add -A` — กู้กลับด้วย `git checkout <ก่อนงาน> -- Html/reconcile.html` แล้วใส่การแก้ที่ตั้งใจใหม่ · **บทเรียน: ห้าม `git add -A` ตอนมีไฟล์เปิดค้างใน preview — ตรวจ `git diff --stat` ก่อน commit ทุกครั้งว่าตรงกับที่ตั้งใจแก้** · mutation 14 แบบ แดงครบ 14 · smoke จริง 656 รายการ/624 SKU |
