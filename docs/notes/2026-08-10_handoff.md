# Note ส่งมอบงาน — 2026-08-10 (ปิดงาน 17:50)

> เขียนตามกติกา admin: หยุดงานก่อน 17:50 → บันทึกว่าทำอะไรแล้ว / กำลังทำอะไร / เหลืออะไร

## ✅ ทำเสร็จวันนี้ (commit + push แล้วทั้งหมด)

1. **Dead code sweep ชุด 1-5** (6 คอมมิต) — asset กำพร้า 4 MB · `<script>` โหลดฟรี 6 จุด · ฟีเจอร์ Extra SKU Drawer −205 บรรทัด · เอกสารล้าสมัย 4 ไฟล์ · เทสยามใหม่ `script-loads`
2. **ถอดฟีเจอร์ SKU Master ออกจากเว็บ** (3 คอมมิต) — ลบหน้า+เมนู+lookup · **ตาราง `sku_master` ยังอยู่ใน Supabase ครบ 1,179 แถว (มติ admin ห้ามลบ)** · แหล่งชื่อสินค้าใหม่ = `fetchBookNamesBySkusAnyCycle` อ่าน `book_stock_lines` ข้ามรอบ · เทสยาม `menu-guard` + `book-names`
3. **แก้ M4** — ชื่อสินค้าเลิกโดน UPPERCASE ตอน import Book (admin ยืนยันแล้วว่า**ไม่ล้างข้อมูลเก่า** — ชื่อ ALL CAPS เดิมคงไว้)
4. **`docs/PURPOSE.md`** (ใหม่) — จุดประสงค์ระบบตามคำอธิบาย admin + workflow 7 ขั้น + ตารางความสอดคล้องความตั้งใจ vs ระบบจริง + **ข้อเสนอปรับปรุง UX/UI/flow 10 ข้อจัดลำดับแล้ว** (รอ admin เลือก)
5. **เทสระบบ** — ชุดอัตโนมัติ **267 PASS / 0 FAIL / 2 KNOWN-OPEN** (เหลือ M2, M19) + smoke เบราว์เซอร์กับข้อมูลจริง (index KPI / reconcile 7 รอบ + lookup ชื่อ / dashboard 335 แถว / เมนู 3 กลุ่ม) ผ่านหมด
6. **Subagent `system-expert`** (ใหม่ — `.claude/agents/system-expert.md`) — ผู้เชี่ยวชาญระบบ รู้จุดประสงค์+workflow+invariants+โครงสร้าง ใช้ตอบคำถาม/วิเคราะห์ผลกระทบ/สอนพนักงานใหม่
   - **เทสแล้ว**: ถาม 2 ข้อ (flow "สร้างลง Book"→เกิน · วิธีเพิ่ม 25→50) ตอบถูกทั้งคู่ อ้าง path:line ตรงจริง รวมเตือน cache-buster เอง
   - ⚠️ ชื่อ `system-expert` จะเรียกผ่าน Agent tool ได้ตรง ๆ **ตั้งแต่ session หน้า** (registry โหลดตอนเริ่ม session — วันนี้เทสผ่านการจำลอง)

## 🔨 ยังไม่ได้ทำ — แผนพร้อมลงมือ (impact สแกนครบแล้ววันนี้ เลขบรรทัด ณ commit ล่าสุด)

### A. ถอดหน้า "คู่มือ" (user_manual) — admin สั่งแล้ว
- `Js/sidebar-shared.js:51` ลบ entry ใน GROUPS (กลุ่ม settings เหลือ 3 ตัว — **ไม่**โดน special-case items.length===1) + `:68` PAGE_FILES · FLAT_PAGES ไม่มี ไม่ต้องแตะ
- ลบไฟล์: `Html/user_manual.html`, `Js/manual-editor.js` (ผู้โหลดรายเดียว), `docs/pages/user_manual.md`, `assets/manual/` ทั้ง 25 ไฟล์, `assets/manual-ui-cycle-config.png`, `assets/manual-ui-reconcile-page.png` (~เกือบ 4 MB)
- **ห้ามลบ `Css/ui-confirm.css`** — อีก 5 หน้าใช้ (audit_check, chat, cycle_config, import_counts, reconcile)
- bump `ASSET_VER` + `?v=` ของ sidebar-shared.js + style.css ทั้ง **11 หน้า**ที่เหลือ (เทส [asset-ver] บังคับ)
- ลบ entry `'user_manual.html': 'manual-editor.js'` ใน `tests/unit/script-loads.test.mjs:62` (ไม่ลบเทสไม่พัง แต่เป็นโค้ดตาย)
- เอกสาร ~15 จุด: CLAUDE.md:47,94 · ARCHITECTURE.md:68,87,95,118,129,131,138 · FIX_TRACKING.md:120,121,124,127,128,134,136,173 (:135 "13 หน้า" ผิดค้างอยู่ ควรเป็น 11) · SYSTEM_GUIDE.md:177 · code-reviewer.md:11-12 + system-expert.md (จำนวนหน้า 12→11)
- ISSUES: ปิด **M20** ทั้งข้อ + ลบ note manual-editor ใน C2 (:32) + ตัด 2 รายการ user_manual ออกจาก **L4** (:305)
- localStorage `stock_audit_user_manual_v2` / `stock_audit_manual_mode_v1` จะค้างในเครื่องผู้ใช้ (ไม่มีใครล้าง — ยอมรับได้ ไม่มีระบบอ่านแล้ว)

### B. ถอด checkbox "ทุกช่วงเวลา (รวมรอบเก่า)" ใน audit_check — admin ยืนยันแล้ว (คือตัวที่เรียกว่า "แสดงทุกคลัง")
- markup `Html/audit_check.html:1115-1118` + CSS `:662-666` (ตายสนิทหลังถอด)
- 🔴 **กับดักสำคัญที่สุด**: `:1876` `if (!warehouseEl || !monthEl || !allTimeEl) return;` — ถ้าลบ element เฉย ๆ `initAuditFilters()` **ล้มทั้งฟังก์ชันแบบเงียบ** → dropdown คลังไม่ sync registry · ต้องแก้ guard นี้พร้อมกัน
- JS 14 จุดที่อ้าง `allTime`: `:1470, 1776-1783, 1822-1825, 1842-1844, 1854, 1867, 1875-1892, 3623-3631, 4080, 4279-4299, 4375-4389, 4415-4453, 5371, 5385-5393, 5397` — พฤติกรรมหลังถอด: กรองตามเดือนที่เลือกเสมอ (default เดิมอยู่แล้ว)
- ลบ localStorage `audit_check_all_time` (เขียน `:1867` อ่าน `:1879`) + แก้ ARCHITECTURE.md:112, docs/pages/audit_check.md:9,80,133
- เทส: ไม่มีตัวไหนอ้าง — ปลอดภัย · ไฟล์นี้แก้ inline อย่างเดียว ไม่ต้อง bump cache-buster

### C. เคลียร์ไฟล์/โค้ดส่วนเกินที่เหลือ (dead code ชุด 6-9 — รายการเต็มใน ISSUES L1/L2)
- `reconcile-shared.js` 6 ฟังก์ชันตาย ~180 บรรทัด — ⚠️ ต้องลด `shared-smoke.test.mjs:32` เกณฑ์ `>= 80` → `>= 75` พร้อมกัน
- `ui-confirm-modal.js` (`parseMessage`) · `live-count-wall.js` 3 จุด · dead handler `btnVerifyAll`/`btnLoadCounts` ใน audit_check
- CSS ตาย ~200 บรรทัดใน `style.css` (บล็อก KPI + sidebar-flat) — บังคับ bump ASSET_VER ทั้งระบบ ควรทำรวบเดียวกับ A
- ⛔ **ห้ามลบ `@import` ฟอนต์ Outfit ใน style.css:1** (มี `<link>` แค่ 4 หน้า แต่ CSS ใช้ 12 จุด)

### D. ข้อเสนอ UX/UI ที่รอ admin เลือก — `docs/PURPOSE.md` หัวข้อ "ข้อเสนอปรับปรุง" (10 ข้อ)
ตัวเด่น: ล็อกคลังใน index ตามรอบที่เปิด · ปุ่ม Export ใน book_explorer · แถบชื่อรอบทุกหน้า · stepper 7 ขั้น · **M2 ต้องตัดสินใจ** (สถานะ Book 0+นับเจอ: DB ให้ count_only แต่ความตั้งใจ admin คือ "เกิน" — ควรยึด JS แก้ SQL)

## ลำดับแนะนำพรุ่งนี้
1. B (เล็ก จบไว ไฟล์เดียว) → 2. A+C รวบเดียว (bump ASSET_VER ครั้งเดียว) → 3. ให้ admin เลือกข้อเสนอใน PURPOSE.md
